1. match_suggestions table — yes, add this to Phase 1 as a prerequisite before the other tasks. Schema: id, sessionId, courtId, suggestedAt, pendingUntil (suggestedAt + 90 seconds), status (pending / approved / playing / completed), approvedBy (userId or "auto"). Players stored as a related table match_suggestion_players with (suggestionId, playerId, team).
2. Check-in contract — player check-in does two things atomically in a single database transaction: flip bookings.status to attended AND insert a queue_entries row at the end of the queue (highest existing position + 1). If a queue_entries row already exists for this player in this session, skip the insert and only flip attendance status — no duplicates. Shannon's panel will show the player appearing in the queue automatically. Shannon retains the ability to manually reorder or remove players from the queue as she does today.
3. Auto-approve safety net — move Shannon's override panel (Phase 3 Task 10) into Phase 1 alongside the timer. They ship together. The timer does not go live without the override UI.
4. Idempotency — add a unique constraint on (courtId, matchSuggestionId) in game_results. One score submission per match. If a duplicate submission arrives, return the existing result silently.
5. Phase 1 Task 4 flag endpoint — this is a new marketplace-authenticated POST route that writes to the existing score_disputes table. Do not build a parallel dispute system. Shannon's existing resolution view handles it from there.
6. Claude failure fallback — if the Claude API call fails or times out during matchmaking, fall back to the existing standard algorithm. Do not auto-approve an empty suggestion — prompt Shannon to assign manually instead.
7. Session end — show the session summary when either sessions.status flips to ended, or the player taps "I'm done for today" on the waiting screen.
8. Score read-modify-write — the Drizzle transaction in Phase 1 Task 1 must wrap the full read-modify-write cycle for all 4 players' skill scores, not just the write. Read current scores, calculate new scores, write all 4 players and all 4 game_participants rows inside a single transaction. This prevents concurrent submissions on different courts from losing updates on players who appear in back-to-back games.
Ready to receive Phase 1 Prompt 1 — the match_suggestions table and Drizzle transaction.