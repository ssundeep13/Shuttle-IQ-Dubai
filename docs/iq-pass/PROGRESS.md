# IQ Pass — progress

**Current gate:** 15 — re-book guard root cause FIXED and LIVE (`297abb2`, then `ae54a17` + `97e75a5` for My games; Railway `c9c95083…` SUCCESS / `4a14067b…` SUCCESS / `338d8d4b…` SUCCESS): a repeat booking request reuses the first booking and its intent, a young intent is never cancelled, guard cancels are stored as `rebook_superseded`, and a payment landing on a superseded row is restored or flagged; My games 375 px width fixed; ten paid-but-cancelled bookings found by the read-only scan (AED 539, every one a double charge) — awaiting Sandeep's per-case decision; IQ Pass selling (6 passes, AED 1,472); 48 h monitor to 2026-09-16 ~14:30Z · **Tests:** 1573/1573 (128 files) · tsc 28 · **Hard stop pending: none** · **Next action for Sandeep:** decide the ten refunds (table below); the deploy gate now checks live sessions before every push

Branch `feature/iq-pass` (from `railway-migration` @ `73e44b2`). Gate 0 is cherry-picked to `railway-migration` and deployed on its own; everything else stays on the feature branch until Gate 8.

## Deploy gate — no push during a live session (Sandeep, 2026-09-15)

Before every push (Railway auto-deploys `railway-migration`) or Railway variable change: run `session-window-check.mjs` (a session in progress, or starting within 30 minutes, blocks; wait until it ends) and record the line here.

- 2026-09-15 07:52Z — before pushing `297abb2` (re-book guard root cause): CLEAR — no session in progress or starting within 30 minutes (11:52 Dubai)
- 2026-09-15 08:01Z — before pushing `ae54a17` (My games inner stacks): CLEAR — no session in progress or starting within 30 minutes (12:01 Dubai)
- 2026-09-15 08:07Z — before pushing `97e75a5` (My games: every grid declares its track): CLEAR — no session in progress or starting within 30 minutes (12:07 Dubai)
- 2026-09-15 08:11Z — before pushing the Gate 15 log + screenshots: CLEAR — no session in progress or starting within 30 minutes (12:11 Dubai)

## Next session

- **Ten refund decisions** (table in the Gate 15 entry) — Sandeep decides per case; nothing has been rescued or refunded.
- `CheckoutCancel.tsx` auto-POSTs a player cancel on mount with no ref guard (a webview reload re-cancels) — Found, not fixed.
- A young pending drop-in has no player-facing "change my booking" path: a differently-shaped repeat inside the 4-hour window answers 409 (by the spec: never cancel a young intent); the 409 carries `redirectUrl`, so a "Continue payment" button on that error is the natural next step.
- Still open from earlier: Resend check for the IQ Pass "active" sends; no shared z-index scale; captain roster shows an IQ Pass seat as "Card"; the admin sheet's dead Confirm/Release buttons on hold seats; pack refunds netted nowhere in finance.

## Rulings applied (Sandeep, 2026-09-14)

- E1(a) `payments.pack_id` + `payments.booking_id` nullable — pre-approved, dry-run output to be saved here, no stop.
- E2(a) per-seat allocation in `bookings.amount_aed` (47 / 45 / 43), `payment_method = 'iq_pass'`.
- E3(a) move cutoff = 5 h before the session being vacated.
- E4(a) Railway staging environment — **hard stop when it needs creating**.
- E5(a) Rankings tier pill from an overlay endpoint; the 7-key public projection stays.
- E6(a) avatar-dropdown entry + Sessions banner + Profile card; no sixth tab.
- E7 sizes S/M/L/XL/XXL; re-pick credits lapse when the pack completes.
- Amendment 1: one shared "collected revenue" helper replaces the five predicates; a test asserts every call site uses it; no pack code reads `amount_aed` directly.
- Amendment 2: exactly two hard stops — creating the staging environment; the real-money AED 188 test + production flag-on.

## DECISION log

- **DECISION (brand hex):** the brief lists `#003E8C` / `#F5EFE0`, but the existing suite bans exactly those two values in the customer layer (`tests/gate2-typography-brand.test.tsx:141-150`, "zero drifted brand hex") and only allows `#006B5F` on the `tealText` token line. The standing rule says the full suite must be green and flag-off behaviour is proven by the existing suite, so new UI uses the app's brand tokens from `client/src/pages/marketplace/LandingComponents.tsx` (`MKT.navy #002C84`, `MKT.tealText #006B5F`, `MKT.cream #F2ECE1`, `FF_BODY` = Inter). Same family, no drifted literals, no pin loosened. Say the word if you want the tokens themselves changed — that is a separate design gate.

## Gate log

### Gate 0 — webhook capacity-race fix — DONE (2026-09-14, deployed)

- RED: `tests/webhook-capacity-race.test.ts` (12 tests) failed at import (`server/paidBookingGuard` absent), then 11/12 after the splice (my tripwire assumed single quotes; corrected), then 12/12.
- Code: `server/paidBookingGuard.ts` (new, `hasCompletedPayment`), `server/webhookHandler.ts` (race branch records the payment, returns `paid: true`; new export `confirmPromotedBookingIfPaid`), `server/guestSlotRefund.ts`, `server/scheduler.ts`, `server/marketplace-routes.ts` (three promotion sites honour a recorded payment with a "Your spot is confirmed" notification; `initiate-payment` → 409 `already_paid`).
- Bar: full suite 1268/1269 (known flake only), tsc 28.
- Commits: `daf4774` on `feature/iq-pass`; cherry-picked as `9cd941d` on `railway-migration`; Railway deploy SUCCESS 12:00 Dubai; `/api/health` 200.
- **Live check (production, test accounts only, torn down):** Smash Sports Academy 16 Sep (`f06f3e33`, 18 cap, baseline 15 free).
  - A. ZZ tester `confirmed` (legacy cash row) + TEST PLAYER `waitlisted #1` carrying a completed payments row → `POST /bookings/:id/cancel` as ZZ tester → 200, `promoted` = TEST PLAYER; TEST PLAYER booking read back **`confirmed`**, `promoted_at` set, guest slot `confirmed`, notifications = `["Your spot is confirmed"]` only (no "complete payment" notification), payments rows still 1, no other notifications for the test account.
  - B. TEST PLAYER `pending_payment` hold with a completed payments row → `POST /bookings/:id/initiate-payment` → **409 `already_paid`**, intent id untouched (no second intent minted).
  - Teardown: 2 payments, 2 guest slots, 1 notification, 3 bookings deleted; 0 rows reference the test intents; session back to 15 free / 0 test bookings.

### Gate 1 — schema, flag, config read, pure rules — DONE (2026-09-14, feature branch only)

- RED: `tests/iq-pass-rules.test.ts` + `tests/iq-pass-schema.test.ts` failed at import (modules absent) → GREEN 32/32.
- Code: `shared/schema.ts` (`packs`, `jobRuns`, `bookings.packId` / `movedFromBookingId`, `payments.packId`, `payments.bookingId` nullable), `server/iqPass/flag.ts`, `server/iqPass/rules.ts` (tiers, cap, window, `validatePicks`, `canMoveSeat`, `jerseyEligible`), `server/iqPass/routes.ts` (`iqPassConfigHandler`), `server/marketplace-routes.ts` (`GET /api/marketplace/config`), `server/storage.ts` (one type-only null guard in `getPaymentTotalsByBookingIds`), `scripts/one-shot/2026-09-15-iq-pass-v1.mts`.
- Bar: tsc 28 (one new error from the nullable column, fixed by the guard); full suite 1301/1301.
- Commit `04d9d72` on `feature/iq-pass` (not deployed; prod still runs `9cd941d`).
- **Migration `iq_pass_v1` — dry-run saved, then run for real on production** (ruling E1a pre-approved): registry was "not run", `packs` / `job_runs` absent, `payments.booking_id` nullable NO → all 11 statements ran in one transaction → COMMITTED; read-back shows `packs` (19 columns), `job_runs` (7), `bookings.pack_id` / `moved_from_booking_id` nullable YES, `payments.pack_id` YES, `payments.booking_id` **nullable YES**. Production health 200 afterwards; `/api/marketplace/sessions` 200; `/api/marketplace/config` 404 on the old code (as today). Full outputs: scratch `g1-migration-dry-run.txt` / `g1-migration-run.txt` (session temp), key lines reproduced here.
- Flag-off proof: `/api/marketplace/config` answers the same JSON 404 as any unknown `/api` path (`server/index.ts:186-191`), so the route table is unchanged while off.

### Gate 2 — purchase hold, single Ziina payment, atomic confirm, hold expiry — CODE DONE, VERIFICATION WAITING ON STAGING

- RED: five files failed at import (`server/iqPass/{purchase,confirm,jobs,store}`, `server/iqPassEmail`), then 53/54 (one pin adjusted to `lastIndexOf` because the hold-gone branch records its payment before the seat-count check), then 54/54; two follow-ups after the full run (schema test needed the JWT env vars once `routes.ts` pulled in the auth middleware; a `Set` spread tripped the TS target) → full suite **1355/1355**, tsc **28**.
- Code: `server/iqPass/store.ts` (calendar query bounded to the window, `createHold` with `FOR UPDATE` + `validatePicks` under the lock, `confirmTx` all-or-nothing with the RETURNING count check and ONE payments row `{pack_id, booking_id NULL}`, `cancelHold`, `expireHolds`, reconciliation candidates), `purchase.ts` (`buildCalendar`, `startPurchase`, jersey rule, 30-minute hold, intent-failure rollback), `confirm.ts` (hooks once, `iq_pass_active` notification, email), `jobs.ts` (hold expiry every 5 min + waitlist promotion; reconciliation every 10 min, 48 h look-back), `routes.ts` (`createIqPassRouter`: calendar / purchase / confirm poll, all JSON 404 while off), `server/iqPassEmail.ts` + `emailClient.sendIqPassConfirmationEmail` (opening line verbatim, key `iq-pass-confirm/<packId>`), `webhookHandler.ts` (booking → pack (flag on only) → guest), `scheduler.ts` (jobs under `isIqPassEnabled()`), `ziinaReturn.ts` (`pack_id` on every return URL), `marketplace-routes.ts` (router mounted with the private resume-token / deep-link helpers injected; six pack-seat guards: cancel, initiate-payment, cash-paid, payment-not-received, admin-confirm, admin-promote).
- Commit `b75b272` on `feature/iq-pass` (pushed). Production still runs `9cd941d`; nothing pack-related is deployed there.
- Flag-off proof: the pack store is never consulted by the webhook while off (test), the scheduler list is unchanged while off (pin), every pack route answers the standard JSON 404 (HTTP test), the return URLs are byte-identical without `packId` (test), and the six guards only fire on rows with `pack_id` (none exist).
- **Staging verification (pending):** script ready at the session scratchpad `g2-staging-verify.mjs` (`config` → `purchase` → pay with a Ziina test card → `poll` → `verify` → `expiry` → `teardown`).

## HARD STOP 1 — create the Railway staging environment (ruling E4a)

Why now: Gate 2's exit criterion is a real purchase over HTTP with a Ziina **test-mode** intent, and the deployed app can only mint test-mode intents when `NODE_ENV !== 'production'` (`server/ziinaClient.ts:156`). Railway has one environment (production). I will not create a billable environment on your account.

What you do (about five minutes in the Railway dashboard, project ShuttleIQ):
1. **Environments → New environment → name `staging` → "Duplicate" from `production`.** This copies both services (`shuttleiq-app` + Postgres) with their variables. The new Postgres starts empty — I seed it from a production dump afterwards (Docker `postgres:18` `pg_dump` → `pg_restore`, read-only against production).
2. In the **staging** `shuttleiq-app` service: **Settings → Source → branch `feature/iq-pass`**, and **Settings → Networking → Generate Domain** (any `*.up.railway.app` name is fine).
3. Reply **"staging created"** (paste the generated staging domain if handy; nothing secret). I will then set the staging variables myself with the CLI (`NODE_ENV=staging`, `IQ_PASS_ENABLED=true`, `REPLIT_DOMAINS=<staging domain>`, everything else inherited), seed the database, run the Gate 2 verification with TEST PLAYER and a Ziina test card, tear down, and continue with Gate 3.

Cost: one extra web service + one Postgres while it exists (usage-based, small); I will remind you to delete it after Gate 8.

### Gate 3 — finance: one classifier, IQ Pass sales line, reconcile — DONE (2026-09-14, feature branch; verified against production data)

- RED: `tests/iq-pass-finance.test.ts` failed at import (`server/revenueClassifier` absent) → after the splice 64/66 (my call-site pins looked for `isCollected(` but the sites pass the function by reference) → 66/66. Two knock-ons: a double comma my import regex left in `portalRoutes.ts` (broke three portal test files at load), and `tests/bank-transfer-confirm.test.tsx:146` pinned the literal predicates the amendment replaces — re-pinned to the classifier (`COLLECTED_METHODS` / `CARD_METHODS` contain `bank_transfer`; both storage blocks use `isCollected`). Full suite **1377/1377**, tsc **28**.
- Amendment 1 delivered: `server/revenueClassifier.ts` (`classifyRevenue`, `isCollected`, `isUnpaidCash`, `isCardTender`, `isIqPassTender`) is used at every site — `sessionProfit.ts` bucket switch + refund lookup, `getPublicAnalytics` per-session / monthly / totals / card bucket, `getFinanceSummary` collected / pending / monthly — and the test asserts no `'ziina'` literal survives in those slices. No pack code reads `amount_aed`.
- Side effects worth knowing: the per-session public analytics block and the admin finance summary now count `bank_transfer` as collected (they had missed the BT1 update — a correction, not a behaviour change for any other method); the refund lookup in `sessionProfit` now also nets refunds on bank-transfer and pack seats.
- Portal: `loadPackRevenueRows` (paid packs, Dubai day of `paid_at`, from the epoch) + `aggregatePackRevenueByMonth`; `/api/portal/finance/pnl` emits `iqPassRevenueAed` + `iqPassByTierAed` (informational, never in net — a pass is already inside collected revenue via its seats' allocation); `client/portal/pages.tsx` shows "IQ Pass sales (info)" with the tier split on hover and a footnote. Reconcile: pack payment rows match the pack price; a pack seat that took a guest charge compares against amount − allocation (`expectedCardFilsForBooking`); `loadReconcileInput` joins `packs` twice. Admin bookings panel badge reads "IQ Pass" for pack seats.
- Commit `4e03bcd` on `feature/iq-pass`.
- **Verification (production database, feature-branch code run locally, one TEST PLAYER row, deleted):** Smash 16 Sep (`f06f3e33`): public totals collected 98 → **145**, `byPaymentMethod.iqPass` 0 → **1 booking / AED 47**, per-session collected 98 → **145**, portal session profit revenue 9,800 → **14,500 fils**, admin finance summary (this month) collected 12,397 → **12,444**; `loadReconcileInput` (1,377 payments) and `loadPackRevenueRows` (0 paid packs) ran cleanly against the live schema. Teardown: 1 row deleted; 0 `iq_pass` rows remain on production.

### Gate 4 — moves, re-picks, session-cancel hook, priority waitlist — CODE DONE (2026-09-14, feature branch; staging verification pending)

- RED: `tests/iq-pass-moves.test.ts` failed at import → 59/63 after the splice (one reversed pin wording; three HTTP failures traced to `vi.restoreAllMocks()` in the email block wiping the `vi.fn()` deps of the HTTP block — probe showed the router itself was fine) → 63/63 with the Gate 2 guards/confirm/purchase files. Full suite **1401/1401**, tsc **28** (one `Booking` type import added after the first run).
- Code: `server/iqPass/moves.ts` (`moveSeat`, `repickSeat`, shared target checks), `rules.ts` (`sortWaitlistWithPriority`, identity when nobody has priority), `store.ts` (`getSessionForMove`, `hasActiveGuests`, `moveSeatTx`, `repickSeatTx`, `getPlusTierUserIds`, `getMyPacks`; `confirmTx` pre-check: a hold whose seats were lost mid-hold is `hold_gone` — payment recorded, refund queued, hold + remaining seats cancelled as `seats_lost`, nothing confirmed), `routes.ts` (`POST /iq-pass/bookings/:id/move`, `POST /iq-pass/packs/:id/repick`, `GET /iq-pass/me`), `storage.ts` (`getWaitlistedBookingsForSession` → `sortWaitlistWithPriority` under the flag only, new `getPlusTierUserIds`; `cancelBookableSessionAndRefund` → `repick_credits + 1` + `iq_pass_repick` notification for pack seats inside the same transaction, `iqPassRepickCount` in the result), `marketplace-routes.ts` (router `moves` / `me` deps; admin session-cancel email passes amount 0 + `iqPassRepick` for pack seats), `emailClient.ts` (`iqPassRepick` block: "free re-pick", no refund wording).
- Commit `d94c59c` on `feature/iq-pass`.
- **DECISION (move targets):** the brief says "unlimited one-step moves" without bounding the destination; a move may target any upcoming, linked session inside the rolling 4-week window from today (the same calendar the player sees), with at least one free spot. The 50% pack cap is not re-checked on moves (purchase-time only, per the brief).
- **DECISION (seat with guests):** a pack seat that has an extra guest attached cannot be moved (409 `move_with_guests`); the guest is cancelled through the existing guest flow first. Moving the guest's money across sessions silently would corrupt the guest-slot refund maths.
- **DECISION (session cancelled during a hold):** rather than the confirm transaction throwing forever on a pack whose held seats vanished, the money is recorded, a `refund_required` row is queued and the hold is cancelled (`seats_lost`) — the same outcome as a lapsed hold, so nothing is ever confirmed partially and no payment goes invisible.
- Flag-off proof: the priority sort is behind `if (!isIqPassEnabled()) return rows;` (pinned); the session-cancel block only fires on rows with `pack_id`; the email variant only with `iqPassRepick`; the three routes 404 while off (HTTP test).

### Gate 5 — tier tag on Profile, Who's Playing, Play, Rankings — CODE DONE (2026-09-14, feature branch; staging screen check pending)

- RED: `tests/iq-pass-tag.test.tsx` failed at import → GREEN 64/64 with the rules and badge-surface files after the splice. Full suite 1417/1418 with one pin knock-on: `tests/badges.test.ts:135` slices 2,200 chars of the `/auth/me` route and the IQ Pass block pushed the badge keys past it — window widened to 3,200, assertions unchanged → **1418/1418**, tsc **28**.
- Code: `shared/iqPassTiers.ts` (labels + order + `highestTier`, imported by the server rules and the client tag — one source), `client/src/components/marketplace/IqPassTag.tsx` (IQP tokens only, no emoji, no price, nothing for a missing tier), `client/src/hooks/useIqPass.ts` (`useIqPassEnabled` via `/api/marketplace/config`, 404 = off; `useIqPassTiers` overlay, requested only while on), `server/iqPass/store.ts` (`getActiveTierByPlayerIds`, `getActiveTiersPublic`, `getActiveTierForUser`), `routes.ts` (`GET /iq-pass/tiers`, public, 404 while off), `marketplace-routes.ts` (`/auth/me` `iqPass` summary, Who's Playing `iqPassTier`, current-suggestion `iqPassTier` — each emitted only under `isIqPassEnabled()`, key absent otherwise), the six surfaces (`Rankings` podium + list via the overlay map, `SessionDetails`, `Play`, `PlayingScreen` after the untouched `BadgeTag` line, `Profile` name row + IQ Pass card → `/marketplace/iq-pass`, `MyScores` SIQ row), `MarketplaceAuthContext` type.
- Commit `6dee3d6` on `feature/iq-pass`. Ruling E5a honoured: `PUBLIC_PLAYER_KEYS` untouched (pinned).
- Flag-off proof: config 404 → hook reports off → no overlay request (jsdom test); server payload keys are spread in only under the flag (pinned); the tag renders nothing without a tier.

### Gate 6 — purchase page, move dialog, pack-aware My Bookings, checkout returns, nav — CODE DONE (2026-09-14, feature branch; staging screen check pending)

- RED: `tests/iq-pass-ui.test.tsx` failed at import → 25/31 after the splice (the fixture lacked the new `tiers` block the page reads; a banner pin scanned the whole Sessions page, which already contains "saving" copy; my page header comment said "savings") → **31/31** with the purchase file. Full suite **1430/1430**, tsc **28**.
- Code: `client/src/pages/marketplace/IqPass.tsx` (tiers → picks → "Your month is locked" → one purchase POST → Ziina; pass management with Move / free re-pick / "Buy your next pass"; flag off renders a plain "not available" notice and requests nothing), `client/src/components/marketplace/IqPassMoveDialog.tsx` (shared calendar picker + move POST, copy per server error code), `MyBookings.tsx` (own "IQ Pass" section, no cancel / pay-now / amount on pack seats, tier label, Move when the server says the seat can move, holds shown as "awaiting payment"), `CheckoutSuccess.tsx` (`pack_id` → pack confirm route, refreshes the pass; heading "Your IQ Pass is active"), `CheckoutCancel.tsx` (never cancels a seat on a pack return; explains the 30-minute hold), `MarketplaceNav.tsx` (dropdown entry under the flag), `MobileBottomNav.tsx` (Sessions tab carries `/marketplace/iq-pass`), `App.tsx` (auth route), `BookSessions.tsx` (one banner under the flag), `server/iqPass/purchase.ts` (calendar payload now carries the player-facing tier table: label, games, pass price — the UI never re-derives money; no allocation).
- Commit `72003bc` on `feature/iq-pass`.
- Copy rule held: jsdom asserts the rendered page never contains "per game", "save/saving" or a per-seat amount; the pass price is the only money shown.

### Gate 7 — renewal job, job_runs ledger, admin jersey handover — CODE DONE (2026-09-14, feature branch; staging check pending)

- RED: `tests/iq-pass-renewal.test.ts` failed at import → **48/48** on the first run after the splice (with the jobs, email and purchase files). Full suite **1444/1444**, tsc **28**.
- Code: `server/iqPass/renewal.ts` (`runIqPassRenewalJob`: renewal in [last − 7 d, last − 1 d] so a missed day still sends; follow-up once last + 7 d with no newer pass; active packs whose last game is past → `completed`; idempotent via the two timestamps; one pack's failure never stops the sweep and leaves its stamp unset for the next run; every run = one `job_runs` row `running → ok | error` with counts), `server/iqPassEmail.ts` (`buildIqPassRenewalEmail` "Your IQ Pass wraps up on Wed 30 Sep — lock your next month", `buildIqPassFollowupEmail` "Ready for another month on court?", keys `iq-pass-renewal/<id>` / `iq-pass-followup/<id>`, no money in the copy), `emailClient.ts` senders, `store.ts` (candidates, `hasNewerPack`, stamps, `markCompleted`, `startJobRun` / `finishJobRun`, `listPacksAdmin`, `markJerseyHandedOver`), `routes.ts` (`GET /api/admin/iq-pass/packs`, `POST /api/admin/iq-pass/packs/:id/jersey-handed-over`, `requireAdmin`, 404 while off; HTTP-tested 404/401/403/200), `scheduler.ts` (`scheduleDailyAtUtcHour(5, runIqPassRenewalJob)` inside the flag block), `client/src/pages/IqPassAdmin.tsx` at `/admin/iq-pass` (ProtectedRoute).
- Commit `3889b7a` on `feature/iq-pass`.
- Draft copy note: the renewal / follow-up wording is mine (the brief fixed only the timing); tweak the strings in `server/iqPassEmail.ts` if you want a different voice — the tests pin the subject lines.

### Gate 8b — IQ Pass terms page `/iq-pass/terms` — DONE (2026-09-14, on `railway-migration`, before flag-on)

- **Ask (Sandeep):** a terms page at `/iq-pass/terms`, brand tokens, Inter, mobile-first, no emoji, flag-gated like the rest, linked from the purchase review screen as "IQ Pass terms" next to the pay button; content verbatim (11 clauses).
- **Tests first (`tests/iq-pass-terms.test.tsx`, 5 tests, RED → GREEN):** the page renders the heading and the eleven clauses verbatim and in order as a numbered list with a back link to `/marketplace/iq-pass`; flag off shows the same "not available" notice as the purchase page and no clauses; source pins — IQ Pass tokens only, `IQP_FONT` (Inter), no hex literal, no emoji, no savings copy, `usePageTitle('IQ Pass Terms')`, `useIqPassEnabled()`; `client/src/App.tsx` routes `/iq-pass/terms` through the public `MarketplaceRoute` wrapper; the review screen (jsdom, Club, four picks → "Your month is locked") shows `link-iq-pass-terms` "IQ Pass terms" → `/iq-pass/terms` inside the same block as `button-pay`.
- **Built:** `client/src/pages/marketplace/IqPassTerms.tsx` (clauses live in one exported array `IQ_PASS_TERMS`), route in `App.tsx`, link in `client/src/pages/marketplace/IqPass.tsx`.
- **DECISION (placement):** "next to the pay button" at phone width — the Pay button already fills its row beside "Change picks", so the link sits directly under Pay inside the same block (teal, 13 px, centred) rather than squeezing a third item into the row.
- **DECISION (route wrapper):** the path is the literal `/iq-pass/terms` (outside the `/marketplace` prefix) as asked; it uses the public `MarketplaceRoute` (nav + layout, no login wall) so the terms can be read before signing in, and the page itself gates on the flag: with `IQ_PASS_ENABLED` unset it shows "IQ Pass is not available right now." exactly like the purchase page.
- **Verification:** full suite 1452/1452, tsc 28; production deploy evidence and the flag-off 404 sweep are recorded under "Merge and production deploy".

### Gate 9 — IQ Pass picker as a calendar — DONE (2026-09-14, `railway-migration`)

- **Ask (Sandeep):** replace the day-by-day list with a month grid (7 columns Mon–Sun, one labelled row per week of the 4-week window), sessions as compact chips ("Smash 20:00"), tap to pick (navy/white when picked), chips at the 50% pack cap greyed and unpickable with a "Pass seats full" tooltip, empty cream cells, "n of N picked" pinned above, "Review" pinned to the bottom once n = N, a horizontally scrollable week strip at 375 px, the old list behind a "List" toggle. Brand tokens, Inter, no emoji, no savings.
- **Tests first (`tests/iq-pass-calendar.test.tsx`, 15 tests, RED → GREEN):** week maths (`buildWeeks` pads mid-week windows, `weekLabel` "14–20 Sep" / "28 Sep–4 Oct"), `shortVenue` ("Bright Riders School Dubai" → "Bright Riders"), `venueColour` from the token palette; pick-mode grid (7 weekday headers, week-row labels, chip buttons with `aria-pressed`, blocked chips `disabled` + `title`, empty cells cream + `data-empty`, out-of-window days marked); the 375 px strip (one week, Prev/Next, swipe via touch events, day rows "Tue 15"); read mode (dots per booking, day buttons, legend); the purchase page (calendar by default, "List" restores the rows, sticky counter, Review bar only at n = N, chips lock at the cap, Club Elite waits for the jersey size, the bar clears the 64 px bottom nav); source pins (IQ Pass tokens only, no hex, no emoji, no icons). The Gate 6 flows now switch to "List" first and assert "Review" is absent (not disabled) before n = N; the terms-link flow picks through chips.
- **Built:** `client/src/components/marketplace/IqPassCalendar.tsx` (one component, `mode="pick" | "read"`, grid ≥ 640 px / week strip below, `useViewportWidth`), `client/src/lib/iqPassTokens.ts` gains `IQP_VENUE_PALETTE` (navy, teal + four muted companions, the only place new hex is written), `client/src/pages/marketplace/IqPass.tsx` picks step: sticky "n of N picked" bar under the 56 px header with the Calendar/List toggle (remembered per device), calendar items with tooltips "Already booked" / "Session full" / "Pass seats full" / "All games picked", the page widens to 960 px while the grid is open, "Review" is a fixed bottom bar (64 px above the bottom nav on phones) that appears only when every game is picked.
- **DECISION (375 px strip):** one week visible at a time as a vertical Mon–Sun panel (seven day rows with wrapped chips) rather than seven cramped columns; swipe left/right or the arrows change week. Desktop keeps the 7-column grid with the week range as each row's header.
- **DECISION (Review bar):** rendered only once n = N (the brief's "pins to the bottom once n = N"); for Club Elite it stays disabled with "Pick your jersey size to review." until a size is chosen, so the jersey rule is unchanged.

### Gate 10 — My Bookings redesign — DONE (2026-09-14, `railway-migration`)

- **Ask (Sandeep):** default view a monthly calendar grid (the Gate 9 component in read mode) with one dot per booked session coloured by venue and a tap-to-open booking card; list view behind a toggle; an IQ Pass strip above the grid (tier · played/total · teal progress bar) linking to the pass page; pass-seat cards with venue, time, Move and "moves close in Xh"; drop-in cards unchanged; past sessions collapsed to one line with the result; empty state linking to IQ Pass.
- **Tests first (`tests/my-bookings-calendar.test.tsx`, 7 tests, RED → GREEN, the page rendered in jsdom for the first time):** calendar by default with the strip ("Club · 1/4 played", bar width 25 %, href `/marketplace/iq-pass`) and the month label, "List" shows the sections with the compact pass-seat card and the untouched drop-in card; dots coloured by `venueColour` (two venues → two colours, cancelled bookings carry none), tapping a day opens the panel: pass seat = venue, time, "IQ Pass · Club", Move, "Moves close in Xh" (X checked against the seat's `canMoveUntil`), drop-in = the existing `card-booking-*` with its AED amount; past rows "… · Attended" / "… · Cancelled" in the list and in the day panel, no full cards for past bookings; no active pass → no strip; empty bookings → `link-empty-iq-pass` while the flag is on and not when off; source pins (the new pieces in one IQ-Pass-token file; My Bookings keeps the drop-in card, wallet chip, error card and cancel flows).
- **Built:** `client/src/components/marketplace/BookingsCalendar.tsx` (`BookingsCalendar` month nav + read-mode grid + day panel, `IqPassStrip`, `PassSeatCard`, `PastLine`, `pickStripPack`, `playedOf`, `movesCloseLabel`, `pastResult`); `client/src/pages/marketplace/MyBookings.tsx`: Calendar/List toggle (remembered per device), the strip above both views, the day panel auto-opens on the nearest upcoming booking of the shown month, list sections unchanged except pass seats (compact card) and Past & Cancelled (one line each), empty state gains the IQ Pass link. `BookingCard`, the wallet chip, the error card, cancel / guest / Pay Now flows and their 17 pinning suites are untouched.
- **DECISION (result on a past line):** the bookings payload carries no game score, so "result" is the booking outcome — Attended (status `attended` or `attendedAt`), Cancelled, Waitlisted, Unpaid, otherwise Booked. Wiring game results (game_results by player) would need a new read and is logged under "Found, not fixed".
- **DECISION (venue colours):** six-colour palette in the token file, assigned by a stable hash of the venue name (a venue keeps its colour across months and devices); with more than six venues two can share a colour — the legend and the day panel disambiguate.
- **DECISION (Add Guest on a pass seat):** kept on the compact card — clause 8 of the terms allows guests on pass seats, so removing the button would contradict the terms.

### Gates 9 + 10 — production verification (2026-09-14 12:40–12:55Z, TEST PLAYER only, torn down)

- **Deploy:** push `97ea7b9` (Gate 9) + `eb73a38` (Gate 10) + `361ad70` (log) → Railway `cca1bf00-838f-4b90-9492-432b51363fa7` SUCCESS 12:40Z; a 502 was seen for a few seconds at 12:40:2x during the rollover (logged under "Found, not fixed"); health 200 and config 200 `{"iqPassEnabled":true}` from 12:40:52Z.
- **Fixture (production, direct rows, no app hooks, no emails):** one active Club pack for TEST PLAYER with pass seats on 18 Sep (Smash) and 19 Sep (Baseline DIP), one past attended seat on 12 Sep (Fire Rallies), one confirmed drop-in on 21 Sep (Smash) — sessions at least four days out, outside the reminder window.
- **My Bookings, 1280 px:** strip "IQ Pass · Club · 1/4 played", bar 25 %, href `/marketplace/iq-pass`; Calendar pressed by default; "September 2026" with Prev/Next; 7 Mon–Sun headers, 5 week rows; four dots — 12 Sep ochre (Fire Rallies), 18 Sep teal (Smash), 19 Sep plum (Baseline), 21 Sep teal (Smash) — legend "Smash · BASELINE DIP · Fire Rallies"; the panel opened on Fri 18 Sep by itself with the pass-seat card "Smash Sports Academy · Fri 18 Sep · 20:00–22:00 · IQ Pass · Club · Move · Moves close in 95h · Add Guest"; tapping 21 Sep showed the untouched drop-in card (AED 49, Ziina, Cancel); tapping 12 Sep showed one line "Sat 12 Sep · 18:00 · Fire Rallies Sports Academy LLC · Attended" and no full card; List: sections IQ Pass (2 compact cards, "Moves close in 95h" / "117h"), Upcoming (1 drop-in card), Past & Cancelled (1 line), strip still above; Calendar restores the grid. Inter throughout, no horizontal overflow, no emoji in the page text.
- **My Bookings, 375 px:** the week strip (14–20 Sep, Prev disabled) with the strip and the same panel; Next → 21–27 Sep with the 21 Sep dot; tapping it opened the drop-in card; List → 2 pass-seat cards + 1 drop-in + 1 past line; no overflow.
- **IQ Pass picker, 1280 px:** the active pass card ("2 of 4 games ahead", Move on both upcoming seats) → "Buy your next pass" → Club → the grid: 5 labelled week rows (14–20 Sep … 12–18 Oct, the next-pass window Sun 20 Sep – Sat 17 Oct padded to full weeks), 24 chips ("Smash 20:00", "Bright Riders 20:00"), the 18 Sep chip greyed "Already booked", 11 empty cream cells, "0 of 4 picked" sticky at `top: 56px` beside the Calendar/List toggle, no Review bar; four taps → "4 of 4 picked", picked chips `rgb(0, 62, 140)` on white, 19 remaining chips locked "All games picked", Review bar `position: fixed; bottom: 0`, enabled; Review → "Your month is locked", "Pay AED 188", "IQ Pass terms" link — no payment made.
- **IQ Pass picker, 375 px:** the week strip (Prev/Next, "Tue 15" day rows, chips 30 px tall), counter sticky; four picks in the 5–11 Oct week → the Review bar sits at `bottom: 64px`, its bottom edge exactly on the bottom nav's top edge (748 px of 812); no overflow.
- **Teardown:** 4 bookings and 1 pack deleted (0 guests, 0 payments, 0 notifications existed); TEST PLAYER on production: 0 live bookings, 0 packs. No real player row was touched.
- **Measurement note (no product bug):** reading the DOM in the same script that dispatched a click showed stale content — React 18 flushes discrete-event updates in a microtask — so the live checks read after a short await; nothing in the app needed changing.

### Gate 11 — IQ Pass picker redesign — DONE (2026-09-14, `railway-migration`)

- **Skill note:** the requested `frontend-design` skill is not installed in this session (only canvas-design and brand-guidelines exist, neither applies), so the design critique below was done by hand against the spec and the brand rules.
- **Design critique of the Gate 9 screen against the spec:** the boxed 7-column grid spent most of its area on empty cells and pushed the venue name into a 12 px chip, the counter and the Calendar/List toggle competed with the picks, and the Review bar only appeared at the bottom of a long page. The spec's week overlines with session cards give each game room for the day, venue, area and time, the slot bar shows progress as the picks themselves, and the review reads as a month rather than a list.
- **DESIGN:** each card shows the weekday abbreviation beside the 22 px day number — a bare number in a row that skips days is ambiguous. **DESIGN:** Booked and Full cards get the same 40 % treatment as cap-reached cards — one disabled language instead of three. **DESIGN:** empty slots read "Game 1 … Game N" so an untouched bar is a visible to-do, not four blank boxes. **DESIGN:** every card and tile carries an aria-label (day, time, venue, area, state) — the visual design uses colour and opacity, so screen readers get the same facts. **DESIGN:** the review keeps the pick lines (day · time · venue, area) under the mini month — coloured tiles alone do not name the venue. **DESIGN:** the page eyebrow "IQ Pass" sits above the tier headline in teal so the single H1 still reads in context.
- **venues.area (one-shot `scripts/one-shot/2026-09-14-venues-area-v1.mts`, key `venues_area_v1`):** dry-run saved to the session scratchpad (`g11-venues-area-dry-run.txt`), then run on production 2026-09-14 13:01Z: `ALTER TABLE venues ADD COLUMN IF NOT EXISTS area text`, 20 of 21 venues populated from their saved locations by district rule (Smash → Dubailand, Bright Riders → Green Community, Baseline → Dubai Investment Park, Fire Rallies → Dubai Investment Park, Al Manara Sports Hall → Al Manara, Al Raya Sports Hall → Al Wasl, Rochester → Dubai Silicon Oasis; the inactive venues → Al Barsha / Al Quoz / Dubai Investment Park / Dubai Silicon Oasis). **Unknown (left NULL): MSA French School, Al Barsha — location "Lycée français Jean Mermoz" names no district (inactive venue; the name suggests Al Barsha, not applied from an address).** Registry row written; Drizzle mirror `venues.area` added; the calendar rows carry `venueArea` (joined by venue name — sessions store the name, not an id) and `/bookings/mine` attaches `venueArea` through one storage read.
- **Shared venue-colour map (`client/src/lib/venueColours.ts`):** exactly four colours — navy `#003E8C`, teal `#006B5F` and the two muted derivatives `#4F6A9A` / `#3E7F76` (added to `iqPassTokens.ts`, white text ≥ 4.7:1 on both); the venues hosting sessions are pinned by name (Smash navy, Bright Riders teal, Baseline navy-muted, Fire Rallies teal-muted, Al Manara navy, Al Raya teal, Rochester navy-muted), anything else hashes into the same four. Used by the picker, the review mini month and Gates 12–13.
- **Tests first (`tests/iq-pass-venue-area.test.ts` 7, `tests/iq-pass-picker.test.tsx` 11, RED → GREEN):** one-shot additive + registered + unknowns listed; Drizzle mirror; `venueArea` on the calendar rows and `/bookings/mine`; the four-colour map and its single consumer; date helpers (weeks, labels, "Mon 14 · 20:00", short venue, countdown); weeks as teal overlines with cards only for days that have sessions (no empty cells, no weekday header); card anatomy (22/800 day number + weekday, 14/600 venue, 12 muted area, 13/500 time, spots caption, 4 px venue rail); cap-reached 40 % + "Pass seats full" caption, Booked, Full, none tappable, no `title` anywhere; the slot bar (N outlines, venue-coloured tiles "Tue 15 · 20:00", tap removes, full bar = one Review button, fifth pick locked); 150 ms animations pinned in source; ≤ 430 px full-width cards with the bar still sticky and no strip; the review mini month (tiles for picks, plain numbers otherwise); the page (the tier headline is the only H1 at 28/800 with the "IQ Pass" eyebrow, no counter, no toggle; review H1 "Your month is locked", tiles, terms link, Pay pinned); source pins (tokens, shared map, no hex, no emoji, no icons, no ramps or lifted shadows, no savings). The Gate 6 flows now pick through the cards and count filled slots; the Gate 9 calendar suite is retired (its component is deleted in Gate 13).
- **Built:** `client/src/components/marketplace/IqPassPicker.tsx` (`IqPassPicker`, `MiniMonth`, `cardState`), `client/src/lib/iqPassDates.ts`, `client/src/lib/venueColours.ts`, `client/src/hooks/useViewportWidth.ts`; `IqPass.tsx` picks step and review step rewired, step-aware H1; `IqPassMoveDialog.tsx` session type gains `venueArea`; server: `store.listCalendarSessions` + `/bookings/mine` + `storage.getVenueAreasByNames`; `shared/schema.ts` mirror.

### Gate 12 — promote IQ Pass (Dashboard + landing) — DONE (2026-09-14, `railway-migration`)

- **Design critique against the spec:** neither surface mentioned IQ Pass — a player found it only through the Sessions banner or the menu. On the Dashboard the promo belongs directly under the greeting, above Getting Started, where the eye lands first and before the session card; once a pass is held the same slot should turn into the pass's progress (one line, no card), so the page does not keep selling to a holder. On the landing page the section is the only place a signed-out visitor sees prices, so the CTA must go through sign-in with a return to the IQ Pass page.
- **DESIGN:** the landing section is a full-width navy band with cream type between the cream hero and the referral promo — the page has no other navy block above the fold, so it reads as a distinct offer rather than another card. **DESIGN:** the three tier cards sit on white inside the band with the price in teal at 22 px — the price is the decision, the perk is the reason. **DESIGN:** a small "IQ Pass terms" link sits beside the landing CTA — a price on a public page needs its terms one tap away. **DESIGN:** one wording for the pass line everywhere, "Club · 1 of 4 played" (the Gate 13 hero uses the same component), so the Dashboard and My games never disagree. **DESIGN:** the Dashboard card carries an "IQ Pass" eyebrow in teal above "Pick your month." — the headline alone does not name the product.
- **Public tier table:** `GET /api/marketplace/config` now answers `{ iqPassEnabled: true, iqPassTiers: [{ tier, label, games, priceAed } × 3] }` while the flag is on (labels, games and pass prices only — no allocation, no per-game figure); the client hook `useIqPassConfig()` reads it once per session and `useIqPassEnabled()` is its boolean view, so every flag-gated screen still answers from the same query.
- **Tests first (`tests/iq-pass-promo.test.tsx`, 9 tests, RED → GREEN):** the config body over HTTP (flag on → tiers; flag off → 404); the promo card (headline "Pick your month.", sub "4, 8 or 12 games. Seats locked.", one line "Club AED 188 · Club Plus AED 360 · Club Elite AED 516", navy "Get your IQ Pass" → `/marketplace/iq-pass`); the progress line ("Club · 1 of 4 played", teal bar 25 %, → pass page); the landing section (H2, sub, three tier cards with games, price and the verbatim perks — Club "Pick any venue", Club Plus "Priority waitlist and first access to new venues", Club Elite "Everything, plus your ShuttleIQ jersey" — one CTA, sign-in first when logged out, terms link); source pins (hook, Dashboard mounts the card or the line under the greeting and before Getting Started, gated on the flag; landing mounts the section after the hero and before the referral promo with the `isAuthenticated` href; tokens, no hex, no emoji, no icons, no ramps, no savings copy). The Gate 1 config pin was updated to the new body shape.
- **Built:** `client/src/components/marketplace/IqPassPromo.tsx` (`IqPassPromoCard`, `IqPassProgressLine`, `IqPassLandingSection`, `IQ_PASS_PERKS`), `client/src/hooks/useIqPass.ts` (`useIqPassConfig`), `server/iqPass/routes.ts` config handler, `Dashboard.tsx` and `MarketplaceHome.tsx` wiring.
- **Copy change (Sandeep, 2026-09-14, after the live check):** the headline on the Dashboard card and the landing band is now "Pick your day. Lock your spot." (was "Pick your month."); the sub "4, 8 or 12 games. Seats locked." is unchanged. Pins updated first (RED → GREEN), both surfaces re-confirmed on production.

### Gate 13 — My Bookings around the next game ("My games") — DONE (2026-09-14, `railway-migration`; replaces the Gate 10 layout)

- **Design critique of the Gate 10 screen against the spec:** the month grid answered "when am I playing?" with dots that needed a tap before they said anything, the one fact a player wants — the next game — sat inside a panel under the grid, and Prev/Next plus the large Calendar/List buttons were chrome. The spec's order is the player's order: the next game first, a scannable strip, then the agenda, with the past folded away.
- **DESIGN:** the hero is a navy card with cream type — the Dashboard's next-session card already uses that treatment, so the two screens agree on what "next" looks like. **DESIGN:** the countdown ticks by the minute, not the second — precision without a jittering number; beyond 72 hours it reads in days ("in 4d") because "in 96h" is not how players think. **DESIGN:** the strip covers six weeks (last week through four ahead) rather than a calendar month — the next game is often next month. **DESIGN:** on desktop the strip turns into a vertical day column beside the agenda (the spec's side-by-side), on phones it scrolls horizontally with today snapped to the centre. **DESIGN:** a row's "Details" opens the existing booking card inline — guest add, drop-in cancel, Pay Now and the refund choices stay exactly where they were, one tap away, without a new dialog. **DESIGN:** past games get no strip tiles — they live under "Played" — so the strip reads as what is ahead. **DESIGN:** the wallet chip stays in the header row as a compact pill (four suites pin it) instead of the removed subtitle.
- **Tests first (`tests/my-games.test.tsx`, 9 tests, RED → GREEN, jsdom render of the page):** the hero (day and date, "20:00–22:00", "Smash Sports Academy · Dubailand", "in Nh" checked against the session start, Move, "Moves close in Xh" checked against `canMoveUntil`, the thin pass line "Club · 1 of 4 played" at 25 % → pass page); one H1 "My games" 28/800 with the grid, month arrows, subtitle and view buttons gone and the wallet chip present; the strip (day letter + number, today `data-today` with the teal underline, tiles with the start time in the venue colour, three tiles for three live games and none for past or cancelled, today centred on load, a tap scrolls to the row); the agenda (two week groups under teal overlines with the range label, rows with day, time, venue, "IQ Pass" chip and Move on pass seats, none of those on a drop-in, Details opens the existing card with Cancel, Add Guest and the amount); Played as a collapsed `<details>` with one line per past game and the outcome; "Nothing booked." with the IQ Pass link and Browse Sessions when nothing is ahead; the desktop two-column layout with the vertical strip; source pins (Gate 10 files gone, MyGames mounted, card / wallet chip / error card / cancel flows kept, no subtitle, tokens, shared map, no hex, no emoji, no icons, no ramps, no savings copy, minute ticker). The Gate 6 My Bookings pins were retargeted to the new structure; the 17 other suites that pin the page are unchanged and green.
- **Built:** `client/src/components/marketplace/MyGames.tsx` (`NextGameCard`, `MonthStrip`, `AgendaWeek`, `AgendaRow`, `PlayedSection`, `EmptyUpcoming`, `buildStripDays`, `groupUpcomingByWeek`, `movesCloseLabel`, `pastResult`, `playedOf`, `pickStripPack`, `useMinuteNow`), `MyBookings.tsx` rewired (H1, no subtitle, hero + strip + agenda + Played, Details toggles), `IqPassProgressLine` gains an on-dark variant for the navy hero. Retired: `BookingsCalendar.tsx`, `IqPassCalendar.tsx`, `tests/my-bookings-calendar.test.tsx`.

### Gates 11–13 — production verification (2026-09-14 13:32–14:1xZ, TEST PLAYER only, torn down)

- **Deploy:** one push `f26c8c5` (Gates 11, 12, 13) → Railway `bfbcd365…` SUCCESS 13:32Z, health 200, `/api/marketplace/config` 200 with the tier table; then the "played" fix `0f1d3c3` → `8fdc68e0-a697-457c-bd60-d54d7184299c` SUCCESS. The migration `venues_area_v1` had run before the code deploy (13:01Z).
- **Gate 12 before the fixture (no active pass), 1280 px and 375 px:** Dashboard shows the promo card under the greeting and before Getting Started — "IQ Pass" eyebrow, "Pick your month.", "4, 8 or 12 games. Seats locked.", "Club AED 188 · Club Plus AED 360 · Club Elite AED 516", navy "Get your IQ Pass" → `/marketplace/iq-pass`, Inter; no progress line. Landing (signed in and signed out): the navy IQ Pass band sits after the hero and before the referral promo — H2 "Pick your month.", the sub, three white tier cards "Club · 4 games · AED 188 · Pick any venue", "Club Plus · 8 games · AED 360 · Priority waitlist and first access to new venues", "Club Elite · 12 games · AED 516 · Everything, plus your ShuttleIQ jersey", one CTA (`/marketplace/iq-pass` signed in, `/marketplace/login?from=%2Fmarketplace%2Fiq-pass` signed out), terms link; at 375 px the cards stack full width (335 px), H2 30 px, no overflow.
- **Fixture (production, direct rows, no app hooks):** one active Club pack for TEST PLAYER with seats on 18 Sep (Smash) and 19 Sep (Baseline DIP), one past attended seat on 12 Sep (Fire Rallies), one confirmed drop-in on 21 Sep (Smash).
- **Gate 12 with the pass:** the Dashboard slot turns into the progress line → `/marketplace/iq-pass`. **Found and fixed during the check:** the first build showed "Club · 2 of 4 played" while the My games hero showed "1 of 4" — the Dashboard derived played as total minus remaining, which counts an unpicked or cancelled seat as played. Fix (`0f1d3c3`, RED → GREEN pin in `tests/iq-pass-promo.test.tsx`): the pass summary now carries `gamesPlayed` (seats whose session has ended — the definition My games uses) and the Dashboard reads it; re-verified live after the redeploy: "Club · 1 of 4 played" on both surfaces.
- **Gate 13 My games, 375 px:** one H1 "My games" 28/800, no subtitle, no month grid, no arrows, no view buttons, wallet chip present; hero "Next game · Fri 18 Sep · in 4d · 20:00–22:00 · Smash Sports Academy · Dubailand · Move · Moves close in 94h · Club · 1 of 4 played" (→ pass page); the horizontal strip with today (14 Sep) underlined in teal and three tiles — 18 Sep "20:00" navy, 19 Sep "18:00" navy-muted, 21 Sep "20:00" navy — none for the past or cancelled games; agenda weeks "14–20 Sep" and "21–27 Sep" under teal overlines with rows "Fri 18 · 20:00–22:00 · IQ Pass · Smash Sports Academy · Dubailand · Move · Details", "Sat 19 … IQ Pass … Move", "Mon 21 … Details" (no chip, no Move on the drop-in); Details on the drop-in opened the existing card with Cancel, Add Guest and "AED 49"; a strip tile tap scrolled the agenda; "Played (1)" collapsed with "Sat 12 Sep · 18:00 · Fire Rallies Sports Academy LLC · Attended"; no overflow. **1280 px:** two columns (`320px` hero + vertical strip beside the agenda), no overflow.
- **Gate 11 picker, 1280 px (Buy your next pass → Club):** the only H1 "Club — pick your 4 games" 28/800 under the "IQ Pass" eyebrow; four empty slots in the sticky bar (top 56 px); four week blocks "21–27 Sep … 12–18 Oct" with six cards each under teal overlines; a card reads "22 · Tue · Bright Riders School Dubai · Green Community · 20:00–22:00 · 24 spots left" with a teal 4 px rail; the drop-in's day shows "Booked" at 40 %; no `title` anywhere, no counter, no view toggle. Four taps → navy cards with cream text and the tick, four venue-coloured tiles ("Tue 22 · 20:00" …), the bar complete with one "Review" button, 19 remaining cards locked; a tile tap removed its pick; Review → the only H1 "Your month is locked", four tiles on a five-week mini month, the terms link, "Pay AED 188" in the fixed bar — no payment made. **375 px:** full-width cards (343 px), the bar sticky at 56 px, no strip, Review full width inside the bar, the Pay bar at `bottom: 64px` with its edge on the bottom nav.
- **Teardown:** the fixture pack and four bookings deleted; TEST PLAYER on production: 0 live bookings, 0 packs. No real player row was touched.

### Gate 15 — the re-book guard root cause; paid-but-cancelled scan; My games 375 px — DONE (2026-09-15, `railway-migration` `297abb2` + `ae54a17` + `97e75a5`, Railway `c9c95083…` SUCCESS / `4a14067b…` SUCCESS / `338d8d4b…` SUCCESS)

- **Read-only scan of production (Ziina GET on every cancelled booking that carries an intent with no recorded payment, since the 2026-06-01 portal epoch): 215 candidates → 203 `requires_payment_instrument`, 2 `requires_user_action`, 10 `completed`.** All ten are the same shape as Vishak's: a second identical request 1–6 s after the first, the guard cancelling the first booking, the player paying the first intent — and every one of the ten then ALSO paid the surviving sibling, so each is a **double charge**, not a lost seat (Dhanya Cyriac's sibling even carries two completed payments). Cancelled-side money AED 539 across ten players; the seat question is moot because each holds a confirmed, paid seat. **Nothing rescued or refunded — Sandeep decides per case.**

| Player | Session (Dubai) | Cancelled-side AED | Booked → guard-cancelled (UTC) | Live sibling | Seat still free |
|---|---|---|---|---|---|
| Darshan | 2026-09-14 20:00 Smash Sports Academy | 49 (Ziina 34) | 2026-09-14 06:19:06 → cancelled 06:19:08Z | confirmed PAID | no |
| suraj menon | 2026-09-03 20:00 Bright Riders School Dubai | 49 (Ziina 49) | 2026-09-03 12:30:48 → cancelled 12:30:54Z | confirmed PAID | no |
| Naveen | 2026-09-01 20:00 Bright Riders School Dubai | 49 (Ziina 49) | 2026-08-31 11:48:35 → cancelled 11:48:40Z | confirmed PAID | no |
| Mohammad Abdul Muwahib | 2026-08-27 20:00 Bright Riders School Dubai | 49 (Ziina 49) | 2026-08-27 13:05:16 → cancelled 13:05:18Z | confirmed PAID | yes |
| Mahima Wadhwani | 2026-08-19 20:00 Smash Sports Academy | 49 (Ziina 49) | 2026-08-19 15:08:19 → cancelled 15:08:24Z | confirmed PAID | no |
| Gururaj D | 2026-08-15 18:00 Fire Rallies Sports Academy LLC | 49 (Ziina 49) | 2026-08-15 04:23:06 → cancelled 04:23:07Z | confirmed PAID | yes |
| Anjali | 2026-07-16 20:00 Bright Riders School Dubai | 98 (Ziina 98) | 2026-07-16 14:13:41 → cancelled 14:13:45Z | confirmed PAID | no |
| Aslam | 2026-07-04 18:00 Al Manara Sports Hall | 49 (Ziina 49) | 2026-07-04 05:36:50 → cancelled 05:36:54Z | confirmed PAID | yes |
| Dhanya Cyriac | 2026-07-04 18:00 Al Manara Sports Hall | 49 (Ziina 49) | 2026-07-02 10:22:22 → cancelled 10:22:23Z | confirmed PAID | yes |
| Avinash Jayan | 2026-06-20 18:00 Al Raya Sports Hall | 49 (Ziina 49) | 2026-06-20 12:14:04 → cancelled 12:14:08Z | confirmed PAID | no |

- **Root cause, fixed (server/rebookGuard.ts + the route, RED tests first):** `decideRebook(existing, request, deps)` is a pure decision the booking route now calls. A repeat request inside the 4-hour payment window **reuses** the existing pending booking and its intent (the same head-count, wallet INTENT and guest set → the route answers with that booking and the SAME `redirect_url`; a different request → 409 `pending_booking_exists` with the booking to pay and honest copy: "book the same spots and guests again to continue that payment"); a booking whose intent is younger than the window is **never** cancelled (a young booking that has no intent yet — the repeat landed in the gap before the intent id was attached — answers 409 too); only a young intent Ziina reports **dead** (declined / failed / expired…) is superseded so the player gets a fresh one. Older than the window, the original guard stands (Ziina check, the 1-second re-check for in-flight statuses, fail-closed 503, `flagPossiblyPaid`). A supersede now writes `cancellation_reason = 'rebook_superseded'` — the first time a guard cancel is distinguishable from a player cancel. **DECISION:** the window is the drop-in payment window (4 h), not "a few seconds": a player who closed Ziina and comes back within four hours gets the same booking and intent back, which is what they expect and what the ten cases needed. **DECISION:** "the same request" compares wallet intent (`applyWallet`) against the row's wallet usage and the guest set (`guestKeyOf`: order-insensitive name|email) — never the row's own amount on both sides — because the reused booking confirms and emails the FIRST request's guests.
- **The money can no longer go invisible (server/webhookHandler.ts, storage):** `confirmZiinaBookingByIntentId` still refuses a player-cancelled row, but a `rebook_superseded` row whose intent Ziina reports paid is **restored** (status `pending`, `cancelled_at`/reason cleared, re-priced to what Ziina CAPTURED — the wallet share was returned at supersede — then confirmed like any paid booking: payment row, guest slot, email, hooks) when the player's seat is still free — superseding an UNPAID duplicate sibling first if that is what holds it (Vishak's exact case, now automatic; the sibling is touched only once the restore is certain, and a seat-holding `pending_payment` sibling gives its spots back to the check). When the seat is not free — the sibling is paid (paid twice), the sibling carries wallet credit, the session was cancelled by admin, or the session is full — the captured amount is recorded on the superseded row, a `refund_required` flag ("Refund needed — paid twice for one session" / "paid on a superseded booking" / "paid on a cancelled session" / "paid after the seat was released") lands in the admin Pending Refunds tab, and the row becomes `rebook_refund_pending` — terminal, so the sweep never later flips "refund it" into "restore and confirm"; the payment row and the notification are each idempotent on their own, so a second delivery adds nothing and a failed notification insert is retried. The reconciliation sweep's candidate query now includes `rebook_superseded` rows only (player cancels and flagged rows stay excluded), so a missed webhook is rescued within 10 minutes. `POST /bookings/:id/confirm` now honours the delegate: it no longer answers `confirmed: true` for a row the delegate refused (that is how the ten players saw "Booking Confirmed!" for a cancelled row) and answers `status: 'paid_flagged', paid: true` for a flagged one. **DECISION:** an unpaid duplicate that carries wallet credit is left alone and the payment flagged — cancelling it from the webhook would lose the credit (the wallet return lives in the route module).
- **The client half (client/src/pages/marketplace/SessionDetails.tsx, Checkout.tsx, CheckoutSuccess.tsx, lib/nativeAuth.ts):** the card button had no `disabled` and the submit re-armed itself in `finally` the instant the redirect was scheduled (on web the page had not unloaded yet; on native it never unloads) — a textbook 1-second re-fire. Now: a `useRef` in-flight lock at the top of `makeBooking`, `disabled={processing}` on the button, and no re-arm after a successful redirect on web (the page is navigating away); on the native shell the Ziina sheet can be dismissed without the return deep link ever firing, so `onCheckoutDismissed` (Capacitor `browserFinished`, a no-op on web) re-arms SessionDetails and Checkout when the sheet closes; the waitlisted answer releases the lock; all three booking POSTs show the server's `message` for a 409 instead of the raw slug; `CheckoutSuccess` stops on the first `paid_flagged` answer with a "Payment received — we could not confirm this booking, our team has been notified, reference <booking id>" state instead of polling ten times and ending on the internal token.
- **My games at 375 px (parked yesterday; `297abb2`, then `ae54a17`, then `97e75a5`):** the parked one-liner — `gridTemplateColumns: 'minmax(0, 1fr)'` on `my-games-layout` (a bare `1fr` is `minmax(auto, 1fr)`) — shipped in `297abb2` and was **not enough**: the production screenshot at 375 with "Played" open was still 435 px wide. A Playwright "outermost offenders" probe on the live page found why, twice: the layout column was 343 px as intended, but its two inner stacks are grids too, with an implicit `auto` track that grows to a child's min-content — the Played row's unwrappable "Sat 12 Sep · 18:00 · Fire Rallies Sports Academy LLC · Attended" (419 px) — and every sibling section stretched with it (`ae54a17` capped those two); on that build the probe then showed the same thing one level down — the row list inside each Played `<details>` is a third bare grid (413/419 px). `97e75a5` makes it a rule instead of a chase: every inline grid under the layout declares its track (six single-column stacks in `MyGames.tsx` — the next-game card, the agenda week list, the agenda row text, both Played lists, the empty card — now `minmax(0, 1fr)`), and `tests/my-games.test.tsx` pins that no grid under the layout is bare. The Played row's text then truncates inside 343 px as it was written to. **Found by the screenshot rule, not by the DOM test** — jsdom does no layout; the probe script (`pw/overflow-probe.mjs`) is the tool for the next overflow report.
- **Tests (RED → GREEN):** `tests/rebook-guard.test.ts` (22: the decision table — none / blocked / reuse / different head-count / wallet intent both ways / guest set / young dead intent / young no intent / old unpaid / old in-flight re-check / 503 fail-closed; `guestKeyOf`; route (bounded by the next route), 409 copy, confirm-path and sweep source pins), `tests/rebook-restore.test.ts` (18: restore when free; supersede the unpaid duplicate then restore — a REFUNDED sibling payment counts as unpaid; paid sibling → flagged "paid twice" and terminal; full session → flagged with the sibling left alone; `pending_payment` sibling gives its seat back; admin-cancelled session → flagged; wallet-credit sibling → flagged; idempotent flag; notification retried independently of the payment row; `rebook_refund_pending` answers paid+flagged with no writes; player cancel still refused; captured AED 34 on flag and on restore, gross fallback when Ziina is unreachable; poll route, sweep and client pins), `tests/rebook-client.test.tsx` (5: `onCheckoutDismissed` web no-op / native once-and-remove; the three POSTs prefer `message`; waitlist + native re-arm pins), `tests/checkout-success-paid-flagged.test.tsx` (1: one fetch, "Payment received", the reference, no token), `tests/hold-controls.test.ts` pin moved with the query, `tests/my-games.test.tsx` — the layout column and, after production proved the layout pin alone insufficient, "no grid under the layout is bare". Full suite 1573/1573 (128 files), tsc 28.
- **Adversarial review (three lenses, every finding refuted or confirmed by a second agent):** 27 raw findings, 22 confirmed (several duplicates), every one fixed test-first in the same commit. **Money (server/webhookHandler.ts):** the restore/flag billed `amountAed` (the gross) after the guard had already returned the wallet share — the helper now asks Ziina what it captured (`retrieveZiinaPaymentIntent(intentId).amount`, fallback gross − wallet share) and uses that for the payment row, `refundAmountFils`, the message and the restored row's `amountAed`; the unpaid duplicate sibling was cancelled BEFORE the seat check (a refusal left the player with two cancelled rows) — the sibling is now touched only once the restore is certain, and a seat-holding `pending_payment` sibling gives its spots back to the check; a restore into an admin-cancelled session (availability still reports seats) now flags "paid on a cancelled session"; a flagged row stayed a sweep candidate ("refund it" could flip to "restore and confirm" ten minutes later) — a flag is now terminal (`cancellation_reason = 'rebook_refund_pending'`, the sweep only picks `rebook_superseded`, the confirm path answers paid+flagged for it without writing); the payment row and the refund notification were coupled under one idempotency key (a failed notification insert was never retried) — each is idempotent on its own now. **Sameness (server/rebookGuard.ts):** the route fed the existing row's own wallet amount back into the request, so the wallet half of "identical" could never fail — the request now carries wallet INTENT (`applyWallet`) compared against the row's usage; and the test ignored guests, so the same head-count with different people would have confirmed and emailed the FIRST guests — the guest set (`guestKeyOf`, order-insensitive name|email) is part of sameness. **Client:** the 409 showed the raw slug `pending_booking_exists` — both booking clients (all three POSTs) prefer the server's `message`, and the server copy no longer promises a Pay-now/expiry that a pending drop-in does not have; the waitlisted answer left the panel locked and spinning — released; on the native shell the Ziina sheet can be dismissed without the deep link ever firing, so the lock never cleared — `onCheckoutDismissed` (nativeAuth.ts, `browserFinished`) re-arms SessionDetails and Checkout, a no-op on web; `CheckoutSuccess` polled ten times over 30 s on `paid_flagged` and ended on the internal token — a terminal "Payment received" state now stops on the first answer with the booking reference. **Tests:** the vacuous sibling mock now proves a REFUNDED sibling payment counts as unpaid; the route source-pin window is bounded by the next route. Not changed: the young-window 409 for a differently-shaped repeat is the spec ("never cancel an intent younger than the window") — the honest copy tells the player to book the same spots and guests again to continue the payment.
- **Deploy gate:** `2026-09-15 07:52Z — before pushing `297abb2` (re-book guard root cause): CLEAR — no session in progress or starting within 30 minutes (11:52 Dubai)`; `2026-09-15 08:01Z — before pushing `ae54a17` (My games inner stacks): CLEAR — no session in progress or starting within 30 minutes (12:01 Dubai)`; `2026-09-15 08:07Z — before pushing `97e75a5` (My games: every grid declares its track): CLEAR — no session in progress or starting within 30 minutes (12:07 Dubai)`
- **After deploy:** health 200; the 05:00Z renewal job wrote its job_runs row (`iq_pass_renewal` ok, started 2026-09-15 05:00:00Z, candidates 6, renewals 0, completed 0, follow-ups 0), read back at 07:54Z by a monitor pass
- **Production evidence (Playwright, TEST PLAYER, each image viewed):** `docs/iq-pass/screens/session-pay-card-{375,1280}.png` (Bright Riders 17 Sep, `button-book-now` → inline panel → "Pay by Card" present, enabled, `siq-press` class, no page overflow; at 375 the button is centred clear of the bottom nav) · `dashboard-chips-{375,1280}.png` (375: page 375 px wide, the chip row scrolls inside itself 343/381; 1280: no overflow) · `my-games-played-{375,1280}.png` (fixture: Played (1) / Not played (1), both sections open — 375: page 375 px wide, the Played rows truncate inside the 343 px column ("Fire Rallies Sports …" + Attended / Cancelled), no sideways scroll — taken on build 338d8d4b (97e75a5) after 297abb2 (435 px) and ae54a17 (435 px, the row lists) both still overflowed on their builds; 1280: no overflow). Fixture: `g11-prod-fixture.mjs create` → torn down (`teardown`, 0 rows left).

### Payment rescue — Vishak MS, 16 Sep Smash, AED 49 (2026-09-14 16:06Z, authorised by Sandeep, read-only first)

- **What the admin showed:** one "Pending Payment" drop-in for Smash Sports Academy 16 Sep, booked 13:42 Dubai.
- **What production held:** three bookings for that session made within one second — 09:42:18Z (intent `ba6f1874…`, never paid), **09:42:51Z (intent `9c43ec5c…`, PAID — Ziina `completed`, 4900 fils)** cancelled at 09:42:52Z, and 09:42:52Z (intent `5bfcb222…`, never paid) left in status `pending`. No payments row, no notification, no confirmation email. A fourth attempt at 09:46Z on the full 14 Sep session was abandoned and self-cancelled.
- **The miss, step by step:** the player's booking request fired twice within a second (double-submit at 09:42:51 and 09:42:52). Each request runs the re-book guard: it looks up the player's existing booking on the session, asks Ziina, and cancels it when the intent reads as terminally unpaid. `isZiinaPaymentTerminalUnpaid` counts `requires_payment_instrument` as terminal — right for an abandoned intent, wrong for one created 0.9 s earlier — so the second request cancelled the first request's fresh booking and minted a third. The player's browser had already been redirected to the FIRST intent's Ziina page, so he paid that one. Its webhook (if it arrived) hit a cancelled booking and the canonical path refused it by design ("never resurrect a booking the player cancelled" — it cannot tell a player cancel from a guard cancel, both set `cancelled_at`); the 10-minute reconciliation sweep excludes cancelled rows for the same reason. AED 49 sat invisible against a cancelled row while the surviving row read "Pending Payment".
- **Rescue (one script under `railway run`, guards: same player, same session, paid row cancelled with the paid intent, survivor `pending` with an intent Ziina reports unpaid, no payments row; dry-run first):** moved the paid intent to the surviving booking (both rows annotated in `admin_note`), then ran `confirmZiinaBookingByIntentId` — the canonical path — which confirmed the booking, wrote the AED 49 payments row (16:06:17Z), confirmed the primary guest slot, sent the booking confirmation email and fired the usual hooks. Nothing was charged. **DECISION:** the paid intent was moved to the surviving row rather than un-cancelling the cancelled one, so the admin's "Pending Payment" line is the row that became paid and no cancelled row was resurrected.
- **Found, not fixed (root cause, needs its own gate):** (1) the re-book guard treats `requires_payment_instrument` as terminally unpaid regardless of the intent's age — a grace window (e.g. do not cancel a booking whose intent is under 30 minutes old unless Ziina reports a terminal failure) would have kept the paid row; (2) the booking submit is not idempotent against a double tap — a client-side in-flight lock and a server-side "return the existing pending booking + intent" for a repeat within seconds would stop the chain; (3) the confirm path and the reconciliation sweep cannot distinguish a guard cancel from a player cancel — a distinct `cancellation_reason` (`rebook_superseded`) would let the sweep rescue paid money on guard-cancelled rows. Watch for the same shape: cancelled row + paid intent + a later `pending` sibling.

### Two small fixes — Dashboard chip row, "Played (n)" — DONE (2026-09-14, `railway-migration` `3861a45`, Railway `ec475cd2…` SUCCESS)

- **Dashboard scrolled 22 px sideways at 375:** the community-feed filter chip row ("All · You · Challenges · Results · Tags") is 381 px wide. `client/src/pages/marketplace/CommunityFeed.tsx`: the row now scrolls inside itself (`overflow-x: auto`, no wrap, `min-width: 0`, `max-width: 100%`, thin scrollbar hidden) and the header row lets it shrink (`min-width: 0`); each chip is `flex-shrink: 0` / `white-space: nowrap` so it never squashes. **DESIGN:** a horizontally scrolling chip row is the phone idiom (the month strip on My games already scrolls the same way); wrapping the chips onto two lines would have pushed the feed down on every phone.
- **"Played (n)" counted cancelled seats:** `PlayedSection` in `client/src/components/marketplace/MyGames.tsx` now counts and lists only attended or completed games (`isPlayed`: outcome Attended or Booked — a confirmed booking whose session is over); cancelled, unpaid and waitlisted past rows keep their trace under a second collapsed **"Not played (m)"** section, and either section disappears when empty. A past `pending` (never paid) drop-in now reads "Unpaid" like a lapsed `pending_payment`. **DECISION:** keep the not-played rows visible rather than dropping them — a cancelled booking's line is the only place on My games a player can still see it.
- **Tests (RED → GREEN):** `tests/feed-density.test.tsx` (the tablist scrolls inside itself, chips never shrink or wrap, the header lets the row shrink) and `tests/my-games.test.tsx` (Played (2) = attended + completed, Not played (2) = cancelled + unpaid, order Played → Not played; only cancelled rows → no Played section). Full suite 1527/1527, tsc 28.
- **Production evidence (Playwright, TEST PLAYER, each image viewed):** `docs/iq-pass/screens/dashboard-chips-{375,1280}.png` — at 375 the page is exactly 375 px wide (was 397) and the chip row reports client 343 / scroll 381 px with `overflow-x: auto`, i.e. it scrolls inside itself; at 1280 nothing scrolls. `my-games-played-{375,1280}.png` (fixture: one attended past game + one cancelled past drop-in, both sections opened for the capture) — "Played (1)" with the Attended row, "Not played (1)" with the Cancelled row, in that order. **Found while capturing, NOT fixed (work stopped on Sandeep's instruction):** with the Played / Not played sections open, My games at 375 grows to 435 px — the `my-games-layout` grid column is a bare `1fr` (= `minmax(auto, 1fr)`), so it stretches to the min-content of a `PastLine` row whose venue name does not wrap; closed, the page is 375 px. Fix parked under "Next session". Fixture rows deleted; TEST PLAYER after: live_bookings 0, packs 0.

### Gate 14 — a hold awaiting payment: "Complete payment" and "Hold expired — pick again" — DONE (2026-09-14, `railway-migration` `20c2090`, Railway `bc406963…` SUCCESS)

- **Why:** Sandeep stopped on purpose at the Ziina page (18:15 Dubai) to see what the app does with an unpaid hold. It showed a dead "Club · awaiting payment" card and nothing on the Dashboard, My games or the booking card; the only way back was to wait for the sweep.
- **Server:** `POST /api/marketplace/iq-pass/packs/:id/resume` (`server/iqPass/routes.ts`, flag gate + auth + marketplace auth, owner check answers 404 like an unknown pack, read-only): a live hold → `{ redirectUrl, holdExpiresAt }` from Ziina's own `redirect_url` of the SAME intent (no new intent is ever minted); a lapsed or swept hold → 409 `hold_expired`; money Ziina already took → `confirmPackByIntentId` (the canonical path, hooks + email) → `{ confirmed: true }`; active → `alreadyConfirmed`; a dead intent → 409 `intent_unavailable`. `getMyPacks` now carries `cancellationReason` so the client can tell a swept hold from any other cancellation. `startPurchase` supersedes a hold whose 30 minutes are up (cancels it with the sweep's reason `hold_expired`, seats freed) instead of answering `hold_in_progress` until the sweep runs — a live hold still blocks. **DECISION:** the resume route never cancels anything (the sweep and the next purchase do), so "Complete payment" can never lose a hold by accident.
- **Client:** one state, `pendingPassOf(packs, now)` in `client/src/lib/iqPassPending.ts` — a live hold is `pending` (even beside an active pass: there is money and a clock on it); with no active pass, the newest pack decides: a lapsed hold or a hold the sweep cancelled reads `expired` for 24 hours after its expiry, anything else is nothing. `holdExpiresLabel` → "Hold expires 18:31" (Dubai wall clock, 24 h). One card, `IqPassPendingCard` (`client/src/components/marketplace/IqPassPending.tsx`, IQ Pass tokens only): pending → "Your Club pass is waiting for payment", "Hold expires HH:mm", **Complete payment**; expired → "Your picks were released", **Hold expired — pick again** → `/marketplace/iq-pass?pick=1` (a button that opens the tier chooser when the page is the picker itself). `useCompleteIqPassPayment` (`client/src/hooks/useIqPassPending.ts`) posts to the resume route and opens the redirect; `hold_expired` refreshes `/iq-pass/me` so the card flips without a reload; the surfaces also tick by the minute so a hold flips to expired on its own.
- **Surfaces:** Dashboard slot (pending card → progress line → promo card, in that order); My games hero slot (the navy variant; a held seat is not "your next game" until paid, so the pending card takes the hero and the empty card stays away while the notice shows); the IQ Pass page (replaces the old "awaiting payment" card; the tier chooser hides while a hold is live and returns when it lapses); the booking card of a held seat ("Hold expires HH:mm" + Complete payment on the older MKT surface, or the pick-again link).
- **DECISION:** a "Hold expired" notice is shown for 24 hours after the hold lapsed and only while there is no active pass — long enough to be seen, never a permanent scar. **DECISION:** ?pick=1 opens the tier chooser straight away so the link means what it says. **DESIGN:** one card component for all four homes so the copy, the clock and the button are identical everywhere. **DESIGN:** the hero-slot variant uses the hero's navy with a white button — the slot keeps its identity and the button is the brightest thing on the screen. **DESIGN:** the expiry reads as a wall-clock time ("18:31") rather than a countdown — a player remembers a time, not a shrinking number. **DESIGN:** the expired card explains what happened in one line ("The 30-minute hold lapsed before payment landed") before offering the way back.
- **Tests (RED → GREEN):** `tests/iq-pass-resume.test.ts` (the route over HTTP behind the real auth: flag off 404, no token 401, other player 404, live → same intent, lapsed/swept 409, paid at Ziina → confirm, active → alreadyConfirmed, dead intent 409; startPurchase supersede vs live hold; store + route pins) and `tests/iq-pass-pending.test.tsx` (helpers; the card in both states, busy/error, brand pins; My games live/lapsed/swept/none incl. the booking card and the resume POST → openCheckoutRedirect; IQ Pass page live/lapsed/?pick=1 and the 409 flip; Dashboard source pins). The new hooks live in their own module because four page suites mock `useIqPass` with a fixed export list. Full suite 1516/1516, tsc 28.
- **Adversarial review (three lenses, every finding refuted or confirmed by a second agent):** 20 raw findings, 12 confirmed, all fixed before commit with a RED test each: (1) the new hooks broke four page suites' fixed-export mocks of `useIqPass` → moved to `client/src/hooks/useIqPassPending.ts`; (2) `<=` treated a hold expiring exactly now as lapsed → strict `<`, matching the sweep; (3) the inline supersede freed seats without offering them to the waitlist → `startPurchase` now promotes every freed session the new purchase is not re-taking (no notification: the player is picking again); (4) `cancelHold` returning null (paid or swept a moment earlier) let a second purchase through → 409 `hold_in_progress`; (5) the resume route 409'd a lapsed hold before asking Ziina, discarding a payment that landed after 30 minutes but before the sweep → Ziina is asked first, the clock second; (6) `declined`/`rejected` intents came back with a dead redirect → in the dead list; (7) an intent minted for the other platform (deep-link return URLs vs https) was handed to a page that cannot land the return → the client sends its platform and the server answers 409 `platform_mismatch`; (8) the pending/expired card rendered over the picker and the review → home step only; (9) "Hold expired — pick again" on the IQ Pass page was a no-op because the tier chooser was already showing → the card stands alone until the button opens the chooser; (10) the page-level minute tick re-rendered My games every minute and remounted the inline BookingCard (open dialogs would close) → `IqPassPendingSlot` owns the tick, the pages read the state at render time; (11) the booking card recomputed the state from one pack and could contradict the hero beside an active pass; the hero exclusion also dropped the held seat when `/iq-pass/me` failed, showing "Nothing booked." above a listed game → one page-level state feeds both, and the exclusion is keyed on it; (12) the booking card's failed Complete payment was silent, its lapsed link was a 20 px inline anchor, and its footer still said "awaiting payment" while the banner said expired → error line, 44 px control, footer "hold expired". Cosmetic: two navy slabs when a live hold sits above a next game → the slot is the light variant then. Refuted (left as is): one shared busy flag across a hold's seat banners (one hold at a time, same action), the expired state suppressed by an active pass (deliberate, pinned).
- **Production evidence (Playwright, TEST PLAYER only, each image viewed before this entry):** one real Club hold started through the purchase route for TEST PLAYER at 15:48Z (Ziina intent unpaid, expiry 16:18Z = 20:18 Dubai). **Pending** — `docs/iq-pass/screens/iq-pass-pending-{dashboard,my-games,iq-pass}-{375,1280}.png`: every surface reports `data-state="pending"`, "Hold expires 20:18", a "Complete payment" button whose centre hit-tests to itself; My games shows the navy card in the hero slot, the four held seats as "Payment due" rows, and the opened seat card with "Hold expires 20:18" + Complete payment (no Pay Now, no cancel); the IQ Pass page shows the card alone, no tier chooser. **Lapsed** (hold_expires_at backdated on the test row) — `iq-pass-expired-*-{375,1280}.png`: every surface flips to "Your picks were released" + "Hold expired — pick again" (href `/marketplace/iq-pass?pick=1`; a button on the IQ Pass page), the seat card's footer reads "IQ Pass · Club — hold expired", no Complete payment anywhere. `iq-pass-expired-pick-again-1280.png`: the link's target opens "Choose your pass" with the card out of the way. **Swept** — the production hold-expiry sweep cancelled the pack at 15:53:05Z (reason `hold_expired`, four seats cancelled; the first real sweep observed on production — Sandeep's own 18:15 hold was swept the same way); `iq-pass-swept-my-games-{375,1280}.png`: the expired card stands alone above the strip, no "Nothing booked." card beside it, the rows gone. Fixture rows deleted; TEST PLAYER after: live_bookings 0, packs 0. Found, not fixed: the Dashboard scrolls 22 px sideways at 375 because the community-feed filter chips row ("All · You · Challenges · Results · Tags") is 381 px wide — pre-existing, not the new card.

### Two bugs at 1280 px — no Pay button on the review screen; My games two-column strip — FIXED (2026-09-14, `railway-migration` `6959de9`, Railway `09b36ef0…` SUCCESS 14:08Z)

- **Bug 1 root cause (systematic-debugging; reproduced with Playwright before any code was touched):** `client/src/components/InstallAppBar.tsx` (mounted in `MarketplaceLayout`) is fixed to the bottom edge at `z-40` on every marketplace page whenever Chrome has fired `beforeinstallprompt` and the bar is not snoozed (7-day localStorage). The review step's Pay bar was fixed to the same edge at `zIndex: 30`, so the install bar covered it at 375 (both sit 64 px above the bottom nav), 768 and 1280. Hit-tests at the Pay button's centre returned `button-install-app` / `bar-install-app`; the 1280 capture showed only "Change picks", "My Bookings" and the install bar. It never reproduced in the in-app browser or in jsdom because neither fires the install prompt.
- **Fix:** the Pay bar is `zIndex: 45` — above the install bar, below the sticky header (`z-50`). One value in `client/src/pages/marketplace/IqPass.tsx`.
- **Test (RED first, `tests/iq-pass-picker.test.tsx`):** at 1280 the Pay button is in the DOM and enabled, the bar is `position: fixed; bottom: 0`, not display-none / visibility-hidden / opacity-0, and its z-index is above the install bar's (read from the InstallAppBar source, pinned at 40) and below 50.
- **Bug 2 root cause:** `client/src/pages/marketplace/MyBookings.tsx` switched to a `320px minmax(0, 1fr)` grid at ≥ 1024 px with `MonthStrip vertical`, so the hero or the empty card sat in the narrow left column with the strip rendered as a column of day letters and numbers, and "Played (n)" floated alone on the right.
- **Fix:** one column at every width — the hero or empty card spans the content width, the horizontal scrolling strip (today centred) sits beneath it, then the agenda weeks, then Played. `MonthStrip` lost its `vertical` mode; `useViewportWidth` is no longer used on the page. Empty state: one navy button "Get your IQ Pass" → `/marketplace/iq-pass` (`button-get-iq-pass`); "Browse Sessions" only when the flag is off.
- **DECISION:** no two-column split at any width. The spec allowed one "unless there is agenda content for both columns"; with the strip horizontal there is never content beside it, so the page has one shape and one set of tests.
- **DESIGN:** the empty card carries one action, not a teal link plus a button — two calls on an empty screen split the tap, and the pass is the product being promoted. **DESIGN:** the strip stays horizontal on desktop because a day-letter column beside the agenda reads as a second list rather than a calendar; the underlined "today" row is the same object on the phone and the desktop. (Supersedes the Gate 13 DESIGN line about the desktop day column.)
- **Tests (RED → GREEN, `tests/my-games.test.tsx`):** the desktop test asserts one column, `data-orientation="horizontal"`, `overflow-x: auto`, and document order hero → strip → agenda → Played; the empty-state test asserts `button-get-iq-pass` "Get your IQ Pass" → `/marketplace/iq-pass` with no Browse Sessions and no old link; a new flag-off test keeps "Browse Sessions" → `/marketplace/book`. Full suite 1491/1491 (122 files), tsc 28.
- **Production evidence (Playwright 1.63 Chromium, TEST PLAYER, 14:10–14:20Z, each image viewed before this entry was written):** `docs/iq-pass/screens/iq-pass-review-{375,768,1280}.png` — install prompt fired; Pay bar computed z-index 45 over the install bar's 40; `elementFromPoint` at the Pay button's centre = `button-pay` at all three widths; "IQ Pass terms" + "Pay AED 188" visible on the bottom edge (above the bottom nav at 375). `my-games-{375,1280}.png` — fixture pack (two seats, one drop-in, one played): strip `horizontal`, 720 px wide at 1280 inside a one-column layout, hero → strip → "14–20 Sep" / "21–27 Sep" → "Played (1)", no horizontal page overflow. `my-games-empty-{375,1280}.png` — past-only fixture: "Nothing booked." + "Get your IQ Pass", strip, "Played (1)". Fixture rows torn down after each capture; TEST PLAYER after: live_bookings 0, packs 0.
- **Verification rule from now on (Sandeep, 2026-09-14):** every UI gate is proven with real Playwright screenshots at 375 and 1280 saved under `docs/iq-pass/screens/<gate>-<width>.png`, committed, and looked at before "verified". Elements existing in the DOM is not verification. Playwright lives in the session scratchpad (`pw/`: `playwright@1.63.0` + `npx playwright install chromium`), not in the repo; the driver signs in with a short-lived TEST PLAYER JWT minted under `railway run` and can fire a synthetic `beforeinstallprompt` to make the install bar render.
- **Skills:** `frontend-design@claude-plugins-official` is now installed (user scope). The Skill tool could not load it mid-session (plugins load at session start), so its SKILL.md was read and applied by hand for Bug 2; it loads normally from the next session.

## Gate 8 checklist — runs the moment staging is reachable

1. Staging config (me): `NODE_ENV=staging` comes from a **Staging-only start-command override in `railway.json`** (`npm start` hard-codes `NODE_ENV=production`, so a variable alone cannot switch Ziina to test mode; the Railway `NODE_ENV` variable stays `production` so the build is byte-identical); `IQ_PASS_ENABLED=true`, `REPLIT_DOMAINS=<staging domain>`; confirm the staging database is not production's (masked hosts + identity probe below); seed the staging Postgres from a read-only production dump (pg_dump/psql inside the Staging Postgres container over `railway ssh` — no Docker on this machine). **DONE 2026-09-14 — see "Staging check — second attempt".**
2. Gate 2 verify (`g2-staging-verify.mjs`: config → purchase → Ziina test card → poll → verify → expiry → teardown). **DONE 2026-09-14 (see "Gate 8 — staging verification"); Ziina test-card step DONE by Sandeep 2026-09-14 (end-to-end proof below).**
3. Gates 4–7 verify (`g4-7-staging-verify.mjs`: me → move → cutoff → pills → cancel-session + repick → renewal run + `job_runs` → admin list + jersey), plus a phone-width screen pass of `/marketplace/iq-pass`, My Bookings, Who's Playing, Rankings, Profile in the in-app browser. **DONE 2026-09-14.**
4. Merge `feature/iq-pass` into `railway-migration` (fast-forward is impossible — the Gate 0 cherry-pick sits on both branches; a merge commit is expected), push, watch the deploy, `/api/health` 200, confirm every pack route still 404s and `/auth/me` carries no `iqPass` key (flag off). **DONE 2026-09-14 — see "Merge and production deploy (flag off)".**
5. **HARD STOP 2:** you set `IQ_PASS_ENABLED=true` on production (or say "flag on" and I run `railway variables set`), then the AED 188 real-money purchase on your own account; I read back pack / seats / payment / email and the finance lines, and monitor for 48 h.

## Staging check — first attempt (2026-09-14, after "staging created")

- **Not found.** `railway environment list --json` and a direct GraphQL query of project ShuttleIQ (`f6a94abd…`) both return exactly one environment, `production` (`895e5ecd…`, created 2026-05-24). The account's only other project (arthadao) also has only `production`. Nothing named `staging` exists as far as the CLI token (workspace "ssundeep13's Projects") can see, so no staging variable could be read and **the DATABASE_URL confirmation is still pending**. Likely causes: the duplicate dialog was not confirmed, or the environment was created in a different Railway account/workspace than the one the CLI is logged into. Nothing was created or changed by me (hard stop respected).
- **Production hosts for the later comparison (masked):** app `DATABASE_URL` host `pos***.railway.internal:5432/railway`; public proxy `caboose.proxy.rlwy.net:25452`. A duplicated environment gets its own Postgres service with a different proxy host/port; I will print staging's masked host here and refuse to run anything if it matches production's.
- **Ziina "test keys":** this integration has ONE Ziina token (`ZIINA_API_TOKEN`) and no separate sandbox key — test mode is set per intent (`test: NODE_ENV !== 'production'`, `server/ziinaClient.ts:156`). On staging I set `NODE_ENV=staging`, so every intent minted there is a Ziina test intent (the staging verify step reads the intent back from Ziina and logs its `test` flag). The registered Ziina webhook URL is production's, so a test intent's event reaches production, where the flag is off and an unknown intent is dropped exactly as today (`webhookHandler.ts` guest path, 200 with `guest_not_found`); staging confirms through the poll route and the 10-minute pack reconciliation sweep. `RESEND_API_KEY` is inherited → real emails, test inbox only.
- **DECISION (continue while staging is sorted):** Gate 3 is server-only finance work verified on production with pure reads plus one test-account row, so it does not depend on the unverified purchase path; I am proceeding with it rather than idling. Gate 2's staging verification runs the moment the environment is reachable, before Gate 4.

## Staging check — second attempt (2026-09-14, environment `Staging`)

- **Found.** `railway environment list` now shows `Staging` (id `5ca35ef5…`, created 2026-09-14 10:41Z), duplicated from production with its own Postgres service; the app service in Staging inherited production's variable values (`NODE_ENV=production`, `REPLIT_DOMAINS=shuttleiq.ai,…`, `IQ_PASS_ENABLED` unset, same Ziina/Resend/JWT secrets); `RAILWAY_PUBLIC_DOMAIN=shuttleiq-app-staging.up.railway.app`.
- **DATABASE_URL host check — the literal rule cannot decide this one:** the app's `DATABASE_URL` in Staging reads `pos***.railway.internal:5432/railway`, the **same string** as production's, because Railway's private DNS name `postgres.railway.internal` is scoped per environment (each environment resolves it to its own Postgres service). So I checked identity one level down, read-only (`db-identity.mjs`): Staging Postgres public proxy `sak***.proxy.rlwy.net:22322` vs production `cab***.proxy.rlwy.net:25452` (differ: **true**); Staging server address `10.193.16.255`, postmaster started 2026-09-14 10:42Z, **0 tables**; production server address `10.143.124.129`, started 2026-08-29, 50 tables / 2,415 bookings / 487 users. **DECISION:** the rule protects against running on production's data; the two are provably distinct instances (different servers, different proxies, empty vs full), so I proceeded instead of refusing on the string match. Nothing on production was written at any point.
- **First Staging build "failed" — diagnosed and fixed (before any staging run):** the build succeeded (`railway logs --build`); the *healthcheck* failed because the container crashed at startup: `relation "admin_users" does not exist` from `rotateDefaultAdminPassword` (`server/auth/storage.ts`) — the fresh Staging Postgres had no schema. Fix = seed the schema (next bullet). A second problem surfaced while diagnosing: `package.json` `start` is `NODE_ENV=production node dist/index.js`, so the planned `NODE_ENV=staging` *variable* would have been overridden and **every Staging Ziina intent would have been real money**. Fix = `railway.json` environment override `environments.Staging.deploy.startCommand = "NODE_ENV=staging node dist/index.js"` (Railway config-as-code; production's `npm start` and `package.json` untouched), pinned by `tests/iq-pass-staging-config.test.ts` (3 tests, RED → GREEN). The Staging `NODE_ENV` *variable* stays `production` so the Vite build is identical to production's. Runtime side effects of `NODE_ENV=staging` checked: `seedAdminUser` creates the two default admin logins only if missing (they exist — `admin_users` is copied), `isSecureContext()` stays true via `REPLIT_DOMAINS`, static serving (`app.get("env") !== "development"`), Ziina `test: true`. Production environment: not touched.
- **Seeded (2026-09-14, over `railway ssh` into the Staging Postgres container — pg_dump/psql 18.6 on both sides; the script refuses a non-empty target):** schema = read-only `pg_dump --schema-only` of production → **50 tables** on Staging. **DECISION (no real player data on staging):** Staging inherits production's Resend key and runs the same schedulers, so a copy of real users/bookings could email real players from staging. Data copied = PII-free reference tables only — `admin_users` 4 (hashed), `venues` 20, `session_series` 6, `sessions` 177, `bookable_sessions` 134, `system_one_shot_migrations` 6, `tags` 38 — plus exactly the two test accounts (`players` 2, `marketplace_users` 2: TEST PLAYER `f7f4d0b6…` and ZZ-SANDBOX-GOODWILL Tester `1e29e044…`). `bookings` 0 · `payments` 0 · `packs` 0 · `job_runs` 0 — every upcoming session on Staging starts fully free.
- **Ziina keys:** one token, identical in both environments; test mode is per intent (`NODE_ENV !== 'production'`), now guaranteed on Staging by the start-command override. The Gate 2 verify script reads every intent back from Ziina and refuses to hand out the pay URL unless `test: true`; it also refuses a `STAGING_BASE` that looks like production.

## Gate 8 — staging verification (2026-09-14, environment `Staging`, TEST PLAYER + ZZ tester only, all rows torn down)

Scripts ran under `railway run --environment Staging --service shuttleiq-app` (JWT/Ziina/Resend from the Staging service env, never printed) against the Staging Postgres public proxy; Node needed `NODE_EXTRA_CA_CERTS` with the staging domain's Let's Encrypt "Root YE" chain (curl trusted it, Node 24's bundled store did not).

- **Config:** `GET /api/marketplace/config` → 200 `{"iqPassEnabled":true}` on Staging; production still 404 (checked at the same time).
- **Gate 2 — purchase hold:** TEST PLAYER, Club, 4 picks inside the 4-week window (calendar reported 24 sessions, `packSeatsLeft` 9/12 = 50% of 18/24) → 200 `packId`; `spotsRemaining` 18,24,18,24 → 17,23,17,23 (one seat reserved per session); pack `pending_payment` 188, four `pending_payment` seats `iq_pass` 47 with `promoted_at` null and four pending guest slots, no payment row yet; one Ziina intent (AED 188.00, `requires_payment_instrument`); the intent's `success_url` carried the Staging domain and `pack_id` (`REPLIT_DOMAINS` took effect).
- **Gate 2 — Ziina test mode, proven three ways:** (1) Railway's resolved manifest for the running Staging deployment shows `deploy.startCommand = "NODE_ENV=staging node dist/index.js"` (`fileServiceManifest`), so the intent was created with `test: true`; (2) Ziina's GET does not echo `test`, but the hosted page rendered the intent with the card form (a production intent for AED 188 would look the same) — so (3) the safety net: Ziina documents that its test cards are declined on any non-test intent, so no real money can move with the card below in either case.
- **Gate 2 — Ziina hosted checkout, BLOCKED for automation:** I filled Ziina's form (test Visa `4242 4242 4242 4242`, any future expiry, any CVV — all four fields validated), but the Pay button stays disabled until a Cloudflare Turnstile bot check completes, and it never completes in the automated browser (no challenge token). I do not bypass bot-detection, so **the card submission itself is a manual step for Sandeep** (about two minutes; steps under HARD STOP 2). Every other Gate 2 assertion was verified as follows.
- **Gate 2 — second purchase during a hold:** 409 `hold_in_progress`.
- **Gate 2 — hold expiry on the live sweep:** `hold_expires_at` backdated → the 5-minute sweep cancelled the pack and all four seats and four guest slots, wrote one `iq_pass_hold_expired` notification ("IQ Pass hold released"), and `spotsRemaining` on the first pick returned to 18.
- **Gate 2 — confirm path (DECISION):** with the card step blocked, a fresh hold was activated by running the real confirm seam `confirmPackByIntentId` (`server/iqPass/confirm.ts`, default deps = real store, notification, Resend email, referral/founding/goodwill hooks) against the Staging database from the repo (`g8-activate-pack.mts`, refuses production by host and by row counts); the only thing skipped is the route's "is the Ziina intent paid?" read. Result: `{"confirmed":true}`; read-back: pack `active`, `paid_at` set, four `confirmed` seats `iq_pass` 47 `promoted_at` null `wallet_amount_used` 0, four `confirmed` guest slots, **one** `payments` row `{pack_id, booking_id: null, amount 188, completed, intent id}`, `iq_pass_active` notification "Your IQ Pass is active", Who's Playing on the first pick includes TEST PLAYER, `/bookings/mine` shows 4 rows with the `packId`, confirmation email sent by Resend (id `57d21162…`) to shuttleiqdubai@gmail.com. The Ziina→confirm handshake itself is what Sandeep's test-card payment proves.
- **Gate 4 — moves:** `/iq-pass/me` lists the pack with `canMove` false for the same-day seat (inside 5 h) and true for the others; `/auth/me` carries `iqPass {packId, tier club, gamesTotal 4, gamesRemaining 4, lastGameDate, repickCredits 0}`. Move of the 15 Sep seat → 200 `newBookingId`; source `cancelled` / `iq_pass_move`, new seat `confirmed` `iq_pass` 47 with `pack_id` and `moved_from_booking_id`; capacity source 23 → 24, destination 18 → 17. Cutoff: session start pushed to 4 h away → 409 `move_cutoff_passed`, session restored.
- **Gate 4 — session cancelled by ShuttleIQ:** admin cancel of a session holding a pack seat → 200 `{bookingsCancelled 2, ziinaRefundCount 0, cashRefundCount 0, iqPassRepickCount 1}`; `repick_credits` 0 → 1; notification `iq_pass_repick` "Session cancelled — pick another game"; no `refund_required` row; re-pick → 200 `newBookingId`, credits back to 0. (The second cancelled booking was a leftover ZZ waitlist row from the first `priority` attempt — see next bullet — so Resend was also asked to email `example.invalid`; harmless.)
- **Gate 4 — Plus+ priority waitlist:** the scripted fixture first tried to waitlist TEST PLAYER on a session where they already held a pack seat and hit `unique_active_booking_per_session` (fixture bug, not product). Re-run with the real ordering function every promotion path uses, `storage.getWaitlistedBookingsForSession`, against Staging rows (`g8-priority.mts`): ZZ waitlisted first, TEST PLAYER second → tier `club`: `["ZZ","TEST PLAYER"]`; pack tier temporarily `club_plus`: `["TEST PLAYER","ZZ"]`. Tier restored, fixture rows deleted.
- **Gate 5 — tags:** Who's Playing row for TEST PLAYER carries `iqPassTier: "club"`; `GET /iq-pass/tiers` maps their player id to `club`; in the browser the session page shows the "Club" tag (`tag-iqpass-player-0`), Profile shows `tag-profile-iqpass` and the IQ Pass card ("IQ Pass · Club / 4 of 4 games ahead"). Rankings had nothing to tag because TEST PLAYER has no game results on Staging.
- **Gate 6 — screens at 375 px (in-app browser, TEST PLAYER):** `/marketplace/iq-pass` renders the active pass with the four seats, Move on the three movable seats, "Buy your next pass"; My Bookings shows the "IQ Pass" section with "Move game" and the "IQ Pass · Club" label, no cancel and no pay-now on pack seats, cancelled pack rows under Past & Cancelled; Profile as above. `document.documentElement.scrollWidth > innerWidth` was false on every page. (The purchase steps tiers → picks → "Your month is locked" → pay are covered by `tests/iq-pass-ui.test.tsx`; the live flow ends at Ziina's page, blocked as above.)
- **Gate 7 — renewal job:** `runIqPassRenewalJob()` run from the repo against Staging (`g8-run-renewal.mts`): last game 17 Sep, today 14 Sep → `{"renewals":1,"followups":0,"completed":0}`, renewal email sent by Resend (id `3262614b…`), `renewal_email_sent_at` stamped, one `job_runs` row `iq_pass_renewal` / `ok` with details `{renewals 1, completed 0, followups 0, candidates 1}`. Admin: `GET /api/admin/iq-pass/packs` → 200 (2 rows), jersey handover → 200 with `jerseyHandedOverAt` (jersey size set on the test pack directly for this check).
- **Teardown:** both packs and every row they created deleted (10 bookings, 10 guest slots, 1 payment, 5 notifications, 2 packs), the ZZ leftover row deleted, the fixture session set back to `upcoming`. Staging after: packs 0 · bookings 0 · guests 0 · payments 0 · notifications 0 · 36 upcoming sessions; the one `job_runs` row is left as ledger evidence. Production was never written.
- **Gate 2 — Ziina → confirm handshake, END TO END (2026-09-14 11:56Z, Sandeep on the hosted page, test Visa 4242…):** fresh Club hold `1e17f725…` for TEST PLAYER (4 seats, capacity 18,24,18,24 → 17,23,17,23), Ziina TEST intent `abba2ded…` (AED 188.00, return URL on the Staging domain). Staging log: the confirm route read the intent from Ziina as `requires_payment_instrument`, then, after the card was submitted, as `completed` → `confirmTx` flipped the pack, "[IQ Pass] pack … active — 4 seats confirmed", confirmation email sent by Resend (id `4950c27f…`); the success page's POST and my poll's POST both landed within the same second and the second answered `{"confirmed":true,"alreadyConfirmed":true}` (idempotent). Read-back: pack `active`, `paid_at` 11:56:41Z, four `confirmed` seats `iq_pass` 47 `promoted_at` null, four confirmed guest slots, **one** `payments` row `{pack_id, booking_id null, 188, completed, intent abba2ded…}`, `iq_pass_active` notification, Who's Playing includes TEST PLAYER, `/bookings/mine` 4 rows with the `packId`, `/iq-pass/me` 200. Ziina reports the intent `completed` (test intent, no money moved). Torn down: payments 1 · guest slots 4 · notifications 1 · bookings 4 · packs 1. The confirm path is therefore verified with a real Ziina intent, not only through the seam run.

## Merge and production deploy (flag off) — 2026-09-14

- `feature/iq-pass` merged into `railway-migration` with a merge commit `941addf` (19 commits; the Gate 0 cherry-pick already sat on both sides and merged cleanly). Full suite on the merged tree 1447/1447, tsc at the 28 baseline. Pushed; Railway deployment `ada5478c-063d-49a4-a6f9-d1f666803ef5` reached SUCCESS.
- **Flag-off proof on production after the deploy:** `/api/health` 200; `/api/marketplace/config` 404 (`{"error":"Not found"}`, byte-identical to any unknown `/api` path); `POST /api/marketplace/iq-pass/purchase` 404; `GET /api/marketplace/iq-pass/me` 404; `GET /api/admin/iq-pass/packs` 404; `/auth/me` for TEST PLAYER carries an `iqPass` key: **no**. `IQ_PASS_ENABLED` is unset on the production service (read back after the deploy). No production data was touched.

## Flag on — production (2026-09-14 12:09Z, HARD STOP 2 step 1, on Sandeep's instruction)

- **Before:** terms-page build `3fb848b` deployed (`b3f5d8bb…` SUCCESS 12:07Z) and re-proven flag-off: health 200; config, purchase, me, calendar and admin pack routes all 404 `{"error":"Not found"}` identical to an unknown `/api` path; `/auth/me` for TEST PLAYER without an `iqPass` key; `/iq-pass/terms` served.
- **Set:** `railway variables --set IQ_PASS_ENABLED=true --environment production --service shuttleiq-app`; read back `IQ_PASS_ENABLED=true`, `NODE_ENV=production`, `REPLIT_DOMAINS` unchanged, Staging untouched. Railway redeployed `3fb848b` as `0070e7ac-b4a7-42c7-b472-f5c36be6f003` → SUCCESS 12:09:33Z; health 200.
- **Start command is the original:** the deployment's resolved manifest (`meta.fileServiceManifest.deploy.startCommand`) is `npm start` (the `Staging` override in `railway.json` is keyed to that environment only). Railway's log store had not ingested this container's boot lines by 12:20Z (both `deploymentLogs` and `environmentLogs` returned nothing newer than 12:08Z — the previous container's `> NODE_ENV=production node dist/index.js` echo and `npm error signal SIGTERM` on rollover are the last lines it holds), so the runtime-log confirmation is by the manifest plus behaviour rather than a boot line.
- **Flag visible to the app:** `GET /api/marketplace/config` → 200 `{"iqPassEnabled":true}`; as TEST PLAYER (read-only GETs) `/iq-pass/calendar` 200 (4-week window, 24 sessions, three tiers), `/iq-pass/me` 200 `{"packs":[]}`, `/auth/me` now carries the `iqPass` key (null — no pass), public `/iq-pass/tiers` 200. The pack routes answering means `createIqPassRouter` mounted and the flag-gated scheduler block (same `isIqPassEnabled()` guard) ran.
- **Terms page renders on production:** `/iq-pass/terms` — title "IQ Pass Terms", `h1` in Inter at `rgb(0, 62, 140)` (= `#003E8C`), eleven `<ol>` items matching clauses 1 and 11 verbatim, back link; at 375 px no element exceeds the viewport.
- **Not touched:** no data written for this step.

## Brand tokens for the IQ Pass screens (checked 2026-09-14)

- App-wide `MKT` (`client/src/pages/marketplace/LandingComponents.tsx:19-29`): `navy #002C84`, `cream #F2ECE1`, `tealText #006B5F`. Against the brief: navy and beige differ, teal matches.
- Per your instruction the IQ Pass screens override: new module `client/src/lib/iqPassTokens.ts` exports `IQP` = navy `#003E8C`, teal `#006B5F`, cream `#F5EFE0`, white, the shared ink scale, and `IQP_FONT` = Inter (`var(--font-sans)`). It lives outside the customer-layer sweep globs (which ban the brief's navy/beige literals inside `pages/marketplace` and `components/marketplace`), and every IQ Pass screen will import it rather than inline hex — pinned by `tests/iq-pass-tokens.test.ts` (3 tests). App-wide tokens untouched. This supersedes the earlier brand DECISION above for IQ Pass screens only.

## Found, not fixed

- **Dashboard scrolls sideways at 375 px:** the community-feed filter chips row ("All · You · Challenges · Results · Tags", `section-community-feed`) is 381 px wide, so the page is 397 px wide on a 375 px phone. Pre-existing; seen while capturing Gate 14. A wrapping or scrolling chip row fixes it.
- **"Played (n)" counts cancelled seats:** My games files every cancelled booking under the Played section (outcome "Cancelled"), so after a swept hold a player with no games reads "Played (4)". Pre-existing Gate 13 behaviour; a separate "Cancelled" line or excluding cancelled rows from the count would read better.
- **No shared z-index scale:** the header is `z-50`, the InstallAppBar `z-40`, the picker's sticky slot bar 20, the Pay bar now 45 — each chosen by hand. Any future bottom-fixed control under 40 will be hidden by the install bar the same way the Pay bar was. One layer token file (header / overlay bars / page bars) would make the order explicit; not changed here (minimum change).
- **Install offer unreachable on the review step:** with the Pay bar above it, the InstallAppBar is fully covered on that one screen; it is still shown on every other marketplace page and is snoozable, so no change.
- **Fixture reminder:** a pack seeded by hand with three seats on a four-game pack is not a real state (every real pack has all its seats picked at purchase); it is what exposed the "played" mismatch and is why the summary now carries `gamesPlayed` explicitly.
- **Railway rollover 502:** `https://shuttleiq.ai` answered 502 for a few seconds at 12:40:2xZ while deployment `cca1bf00…` replaced `0070e7ac…` (health 200 again by 12:40:52Z). Railway's zero-downtime overlap did not hold for that rollover; not changed here.
- **Past rows show the booking outcome, not the game score:** `/bookings/mine` carries no game_results; a "W 21–15" style result on the one-line past row would need a new read keyed by player (Gate 10 decision).
- **Cancelled pack checkout keeps the hold for 30 minutes:** per the plan, a player who backs out of the Ziina page keeps their picks held until the hold lapses (the seats are then offered to the waitlist). A tiny owner-only "release my hold" route would free them immediately; not built (not in the brief).
- **`client/src/pages/marketplace/Checkout.tsx` is orphaned** (no in-app link reaches `/marketplace/checkout/:id`; the live path is the inline panel on the session page). Untouched.
- **Admin bookings badge:** `client/src/pages/SessionsManagement.tsx` shows "Ziina" for every non-cash method, so a `bank_transfer` booking reads "Ziina" (pre-existing since BT1). Pack seats now read "IQ Pass"; the bank-transfer label was left as is.
- **Brand drift (app-wide):** `MKT.navy` is `#002C84` and `MKT.cream` is `#F2ECE1` where the IQ Pass brief specifies `#003E8C` / `#F5EFE0`; `index.css --primary` is also `#002C84`. Every existing marketplace screen uses the drifted values and `tests/gate2-typography-brand.test.tsx:141-150` enforces them. Not changed (out of scope); IQ Pass screens use `IQP`.

- Pre-fix data only: a booking whose `ziina_payment_intent_id` was overwritten by a second `initiate-payment` before this fix has a completed payments row under the first intent. `confirmPromotedBookingIfPaid` looks the booking up by that intent, misses, and returns false; `initiate-payment` now 409s, so the admin path (Confirm Payment) is the way through. No such rows exist today (Owais was repaired by hand on 3 Sep).
- `scheduler.ts` computes `dateLabel` before the promotion branch; unused on the paid path (harmless).


## HARD STOP 2 step 2 — real purchases read back (2026-09-14 14:25–14:45Z, read-only; PII masked; nothing modified)

- **What Sandeep reported:** "paid — real Club purchase completed at 18:01 Dubai, Ziina order #439726, AED 188 on my account."
- **What production holds (packs created after the promo went live, all real players):**
  - 14:01:39Z hold → **paid 14:02:04Z** (18:02 Dubai): **Naveen** (marketplace user `86ff7e65…`, linked player `230a6c27…`) — **Club, AED 188, active**, 4 seats confirmed at AED 47 (Bright Riders 22 Sep, 29 Sep, 6 Oct; Fire Rallies 10 Oct 18:00), one `payments` row (188, completed, `booking_id` NULL, intent `4296f1c6…`), notification `iq_pass_active` "You're now a Club member of ShuttleIQ. Your IQ Pass is active — 4 games locked…". Ziina: `completed`, 18800 fils, message "ShuttleIQ IQ Pass · Club". **This matches the reported time and amount, but the account is "Naveen", not "Sandeep Surendra".**
  - 14:13:41Z → paid 14:13:54Z: **Irianna Gri** — **Club Plus, AED 360, active**, 8 seats at AED 45 (Smash Sports Academy, 16 Sep → 2 Oct), payments row 360, notification. Ziina `completed`.
  - 14:15:56Z: **Sandeep Surendra** (`e1066a42…`, Google-linked, player `9c1b2f1c…`) — **Club, pending_payment**, 4 seats held at 47 (Bright Riders 17/24 Sep, 1/8 Oct), no payments row, no notification. **Ziina intent `45ad5dcd…` = `requires_payment_instrument` (never paid).** Hold lapses 14:45:56Z; the 5-minute sweep will cancel it and free the seats (first real hold expiry — watched by the monitor).
  - 14:21:12Z → paid 14:21:32Z: **SISHIL IMMO** — **Club, AED 188, active**, 4 seats at 47 (Baseline DIP 19 Sep; Fire Rallies 26 Sep, 3 Oct, 10 Oct), payments row, notification.
  - 14:22:05Z → paid 14:22:41Z: **Reena John** — **Club, AED 188, active**, 4 seats at 47 (Bright Riders 17, 22, 24 Sep; Baseline DIP 19 Sep), payments row, notification.
  - 14:34:xxZ → paid 14:35:07Z: **Owais Raza** — **Club Plus, AED 360, active**, 8 seats at 45, payments row, notification (arrived while this entry was being written).
- **Totals:** 5 active packs = AED 1,284 = 28 confirmed seats × their allocation (12 × 47 + 16 × 45 = 1,284) — the no-double-count invariant holds. Public tiers overlay (`/api/marketplace/iq-pass/tiers`) lists the linked players with their tier.
- **Finance portal (real endpoints, 10-minute owner token minted under `railway run`, GET only, read at 14:28Z before the fifth pass):** `/api/portal/finance/pnl` month 2026-09 → `iqPassRevenueAed 924`, `iqPassByTierAed {club 564, club_plus 360, club_elite 0}` (the "IQ Pass sales (info)" line; the same SQL now gives 1,284), `collectedRevenueAed 14460`; `/api/portal/finance/sessions` → the seats sit in collected on their session dates (17 Sep Bright Riders collected 47; 22 Sep 94; 19 Sep Baseline 241 incl. 94 of pass money; 10 Oct Fire Rallies 94). Runner pay (`/api/portal/finance/runner-pay`) values each pass seat at its allocation: Shannon W38 17 Sep Bright Riders value 47, W39 22 Sep 94 / 24 Sep 47 / 26 Sep 47; Preetham 16 Sep Smash 192 (incl. 45), 18 Sep 45.
- **Captain pay / admin sheet:** there is no captain-visible amount view (the captain roster omits amounts by design — see map); the admin "View Bookings" sheet (`GET /api/marketplace/sessions/<bookable>/bookings`, super_admin token) shows the pass seats as `paymentMethod iq_pass · amountAed 47 · packId set` on 17 Sep Bright Riders (Reena confirmed, Sandeep pending) and 22 Sep Bright Riders (Reena + Naveen).
- **Dashboard progress line / My games hero:** not screenshotted — the only account we may sign in as is TEST PLAYER, the "Sandeep Surendra" account has no active pass (its hold is unpaid), and impersonating a real player's session is out of bounds (**DECISION**). From the builders the four Dashboards read "Club · 0 of 4 played" / "Club Plus · 0 of 8 played" and the heroes show the first pick (Naveen: Tue 22 Sep 20:00 Bright Riders). Re-run with Playwright the moment the user confirms which account is theirs.
- **Confirmation email:** no email log table exists and the Railway CLI returned no log history for any of today's deployments (known gap — see memory), so the four "Your IQ Pass is active — <tier>" sends can only be confirmed in the Resend dashboard (idempotency keys `iq-pass-confirm/<packId>`). **Sandeep: please check Resend for four sends at 14:02, 14:13, 14:21, 14:22Z.**
- **Jobs:** `job_runs` is empty — expected: only the daily renewal job (05:00Z) ledgers; hold-expiry / reconciliation log to console only. Reconciliation had nothing to rescue (every paid intent has its payments row).
- **Found, not fixed (from the read-back):** (1) no captain-visible per-seat amount, and the captain roster shows an IQ Pass seat as plain "Card" (`client/src/components/AddPlayerModal.tsx`); (2) the admin sheet still renders Confirm Payment / Release Hold on IQ Pass hold seats even though every endpoint refuses them (`iq_pass_seat`); (3) `bank_transfer`/`wallet` rows read "Ziina" in the admin sheet; (4) a refund on the pack payments row is netted nowhere in finance; (5) the IQ Pass sales line buckets by Dubai day while created_at readers bucket by UTC; (6) no weekly IQ Pass line on the weekly P&L.

## Ziina receipt line — "ShuttleIQ IQ Pass · <first name> · <tier>" (2026-09-14, `f963a64`, Railway `6db4b6ac…` SUCCESS)

- `server/iqPass/purchase.ts`: `iqPassIntentMessage(name, tierLabel)` — first token of the trimmed marketplace name; a blank name drops the segment; never the price or allocation; `sanitizeZiinaMessage` still caps at 50 bytes (longest real case "ShuttleIQ IQ Pass · Krishnachandran · Club Elite" = 50 bytes). Pinned RED → GREEN in `tests/iq-pass-purchase.test.ts` (happy path now expects "ShuttleIQ IQ Pass · Test · Club"; new test for trimming, blank name, Club Elite, byte cap). Suite 1492/1492, tsc 28.
- **Production check (TEST PLAYER, one Club hold through the real purchase route, intent read at Ziina, rows deleted):** Ziina intent message = "ShuttleIQ IQ Pass · TEST · Club" (MESSAGE OK), pack + 4 seats + 4 guest slots deleted, TEST PLAYER after: live_bookings 0, packs 0.

## Staging scaled down (2026-09-14 14:26–14:33Z)

- Goal: stop Staging's schedulers (all 12 run at boot; nine of them regardless of the IQ Pass flag) from emailing through the shared Resend key.
- `railway scale --environment Staging --service shuttleiq-app iad=0` did **not** zero the service: the API dropped the region and re-created the default region at 1 replica (config went `iad:1` → `us-west2:1`; a second `us-west2=0` and an `environment edit … numReplicas 0` behaved the same way, ending at `iad:1`). Two Staging builds that this triggered **FAILED**; the previous deployment was already REMOVED, so **Staging has no running deployment** (health 404) — nothing runs.
- **DECISION:** because the CLI cannot pin zero replicas, the shared key was removed from Staging instead — `railway variable delete RESEND_API_KEY --environment Staging --service shuttleiq-app` (production's key untouched and verified present). A future Staging deploy boots fine (the Resend client is lazy) but every send fails closed. Re-add the key from the dashboard if Staging is ever needed for email checks. Staging Postgres is still running (untouched).
- Production checked after every step: app config `iad:1`, latest deployment SUCCESS, health 200.

## 48-hour production monitor (2026-09-14 14:33Z → 2026-09-16 ~14:30Z)

- `hs2-monitor.mjs` (scratchpad; runs under `railway run`, read-only) once every two hours: pack counts by status, lapsed holds older than 7 min (sweep health), active packs with seats ≠ games_total, active packs without a payments row, completed pack payments on non-active packs, IQ Pass `refund_required` rows, `iq_pass_*` notification counts, `job_runs` (renewal 05:00Z on the 15th and 16th), pending intents cross-checked at Ziina (paid-but-unrecorded = reconciliation must rescue), health, config flag, latest deployment status, Staging still down. Each pass appends to `hs2-monitor.log`; ALERT lines are reported to Sandeep.
- Session cron `d385dac6` fires every two hours at :07 (session-only; dies with this Claude session — **DECISION:** not a cloud schedule, because the pass needs the local Railway login and the local database URL).
- Pass 1 (14:32Z): OK — 4 active / 1 pending (Sandeep's, unpaid at Ziina), health 200, flag on, deploy SUCCESS, Staging down. Pass 2 (14:36Z): OK — 5 active (Owais Raza's Club Plus landed 14:35Z) / 1 pending, deploy `6db4b6ac` SUCCESS.

## HARD STOP 2 — Ziina test-card payment on Staging, then production flag-on + the AED 188 real-money purchase

Everything below is yours. Nothing runs until you say so.

**Step 0 — DONE 2026-09-14 11:56Z.** Sandeep paid the Staging test intent with the Ziina test card; the confirm route confirmed the pack from Ziina's `completed` status; full read-back OK; rows torn down (evidence in "Gate 8 — staging verification").

**Step 1 — DONE 2026-09-14 12:09Z.** `IQ_PASS_ENABLED=true` on the production service, redeploy `0070e7ac…` healthy, config reports the flag on, terms page live (evidence under "Flag on — production"). Sandeep then held the purchase until Gates 9 and 10 are live.

**Step 2 — the real purchase (AED 188, your own account).** On https://shuttleiq.ai open the IQ Pass entry (Profile card, the Sessions banner, or the menu link) → Club → pick your 4 games → "Your month is locked" → Pay → Ziina with your real card. Reply "paid" (or the Ziina reference). I then read back: pack `active` and `paid_at`, four `confirmed` seats at 47 with `promoted_at` null, one `payments` row for 188 with `pack_id` and no `booking_id`, the `iq_pass_active` notification, the "Your IQ Pass is active — Club" email in Resend, the finance figures (+188 collected today, `iqPass` bucket, portal "IQ Pass sales (info)" Club 188, reconcile `iq_pass` intents) and Who's Playing tags on your four sessions — then monitor for 48 hours (hold-expiry sweep, reconciliation sweep, the 09:00 Dubai renewal job's `job_runs` rows).

**If anything looks wrong at any step:** reply "flag off" — I set `IQ_PASS_ENABLED` back to unset, Railway redeploys, and the app is byte-identical to today (pack routes 404, `/auth/me` unchanged); any pack rows stay in the database for inspection and nothing else references them.
